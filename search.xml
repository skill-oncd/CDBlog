<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Welcome to CoolDownSpace — 欢迎来到我的战斗设计自留地</title>
    <url>/2026/08/04/welcome-to-cooldown-space/</url>
    <content><![CDATA[<h2 id="为什么是-CoolDownSpace？"><a href="#为什么是-CoolDownSpace？" class="headerlink" title="为什么是 CoolDownSpace？"></a>为什么是 CoolDownSpace？</h2><p>🎮 我是 CD，一个正在游戏行业摸爬滚打的 <strong>Combat Designer</strong>（战斗策划）。</p>
<p>这个博客的名字是个双关：</p>
<ul>
<li><strong>CoolDown</strong> — 技能冷却。我每天的工作就是调整技能的 CD 时间，直到战斗手感<em>对了</em>为止</li>
<li><strong>CountDown</strong> — 倒计时。因为项目的 ship date 永远在倒数</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 我的人生状态机</span></span><br><span class="line"><span class="keyword">while</span> (Alive)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">DesignCombat</span>();</span><br><span class="line">    Skill.OnCooldown = <span class="literal">true</span>; <span class="comment">// 暂时的——下次迭代又不一样了</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="这里会有什么？"><a href="#这里会有什么？" class="headerlink" title="这里会有什么？"></a>这里会有什么？</h2><table>
<thead>
<tr>
<th>栏目</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td>⚔️ <strong>战斗设计拆解</strong></td>
<td>拆经典 ARPG 的战斗系统——技能机制、受击反馈、帧数据</td>
</tr>
<tr>
<td>🛠️ <strong>工程实践</strong></td>
<td>Unreal GAS &#x2F; Unity 技能系统踩坑记录</td>
</tr>
<tr>
<td>💡 <strong>设计思考</strong></td>
<td>为什么有些战斗”爽”，有些就是不对味</td>
</tr>
<tr>
<td>🎯 <strong>求职复盘</strong></td>
<td>面试题、作品集思路、行业洞察</td>
</tr>
</tbody></table>
<p>没有通稿式的”技术分享”，只有一线开发者的真实记录。</p>
<h2 id="当前进度"><a href="#当前进度" class="headerlink" title="当前进度"></a>当前进度</h2><blockquote>
<p><strong>正在做：</strong> 一款 ARPG 的战斗 &amp; 技能系统</p>
<p><strong>引擎：</strong> Unreal (C++ &#x2F; Blueprint) · Unity (C#)</p>
<p><strong>日常关键词：</strong> 技能配置表 → GAS → 受击框 → 帧数据对齐 → 数值调优 → 开会 → 重做 → 继续调</p>
</blockquote>
<h2 id="Why-write"><a href="#Why-write" class="headerlink" title="Why write?"></a>Why write?</h2><p>三个原因：</p>
<ol>
<li><strong>记录成长。</strong> 踩过的坑、想通的道理，写下来才不容易忘</li>
<li><strong>建立信号。</strong> 对于招聘方来说，公开的技术博客是最诚实的简历</li>
<li><strong>连接同类。</strong> 如果你也在做类似的事情，希望这些内容对你有用</li>
</ol>
<blockquote>
<p><code>skill.cooldown = 0</code> — CD 转好了，开始干活 🚀</p>
</blockquote>
]]></content>
      <categories>
        <category>Career</category>
      </categories>
      <tags>
        <tag>portfolio</tag>
        <tag>game-design</tag>
        <tag>career</tag>
      </tags>
  </entry>
  <entry>
    <title>ArcaneRush：从 C++ 基类到蓝图逻辑的 UE5 塔防实战</title>
    <url>/2026/08/06/ArcaneRush-UE5-Blueprint-Portfolio/</url>
    <content><![CDATA[<h2 id="项目概览"><a href="#项目概览" class="headerlink" title="项目概览"></a>项目概览</h2><table>
<thead>
<tr>
<th>项</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong>引擎</strong></td>
<td>Unreal Engine 5.6</td>
</tr>
<tr>
<td><strong>类型</strong></td>
<td>3D Tower Defense（塔防）</td>
</tr>
<tr>
<td><strong>开发周期</strong></td>
<td>约 5 个月（2025 年 6 月 — 11 月），21 次提交</td>
</tr>
<tr>
<td><strong>核心标签</strong></td>
<td>C++, Blueprint, DataTable, Behavior Tree, UMG, Spline</td>
</tr>
<tr>
<td><strong>代码规模</strong></td>
<td>~500 行 C++，8 个类，蓝图总量 150+</td>
</tr>
</tbody></table>
<p>这是我的第一个独立 UE 项目。目标是<strong>把引擎核心功能跑通一遍</strong> —— 从 C++ 基类设计到大规模蓝图逻辑编排，从 Behavior Tree AI 到 UMG 局内外完整 UI。核心定位是”会用 UE 做完整游戏”，而非追求某一技术点的极限深度。</p>
<iframe src="//player.bilibili.com/player.html?bvid=BV1uxuJ68Ept" scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true" style="width:100%;height:480px;"></iframe>

<p>📦 高清下载: <a href="https://easylink.cc/wuspmt">EasyLink云链</a></p>
<hr>
<h2 id="一、架构总览"><a href="#一、架构总览" class="headerlink" title="一、架构总览"></a>一、架构总览</h2><h3 id="1-1-设计思路：C-管数据，蓝图管逻辑"><a href="#1-1-设计思路：C-管数据，蓝图管逻辑" class="headerlink" title="1.1 设计思路：C++ 管数据，蓝图管逻辑"></a>1.1 设计思路：C++ 管数据，蓝图管逻辑</h3><p>初学时面临一个关键选择：逻辑写 C++ 还是蓝图？最终选择 <strong>C++ 定义基类和数据结构 + 蓝图实现全部游戏逻辑</strong>：</p>
<ul>
<li><strong>C++ 层</strong>：类继承体系 + DataTable 结构体 + 伤害公式 + Buff 子系统 + 工具函数</li>
<li><strong>蓝图层</strong>：塔攻击流程、敌人 AI、Hero 技能、HUD 交互、波次调度、存档读取</li>
</ul>
<p>这样分工的好处是：数据结构和性能敏感的计算在 C++ 中类型安全且可断点调试，而频繁迭代的游戏逻辑在蓝图中可视化和快速调整。</p>
<h3 id="1-2-类继承体系"><a href="#1-2-类继承体系" class="headerlink" title="1.2 类继承体系"></a>1.2 类继承体系</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ACharacter</span><br><span class="line">  └── ABaseCharacter      ← 所有角色基类（血量/伤害/护甲/Buff 管理）</span><br><span class="line">        ├── ABaseEnemy     ← 敌人（Spline 寻路 + Behavior Tree AI）</span><br><span class="line">        ├── ABaseHero      ← 英雄（技能 + 复活机制）</span><br><span class="line">        └── ABaseSoldier   ← 兵营士兵（复活 + 独立动画）</span><br><span class="line"></span><br><span class="line">AActor</span><br><span class="line">  └── ABaseTower          ← 塔（多级升级 + 投射物 + 范围检测）</span><br></pre></td></tr></table></figure>

<p>关键设计决策：</p>
<ul>
<li><strong>塔继承 AActor 而非 ACharacter</strong>：塔不需要 CharacterMovementComponent，轻量级 Actor 更合适</li>
<li><strong>DataTable 属性嵌入到每个子类的专用结构体</strong>：敌人的 <code>FEnemyProperty</code> 里嵌套 <code>FCharacterBaseProperty</code>，每个子类只暴露自己需要的额外字段</li>
</ul>
<h3 id="1-3-DataTable-数据驱动"><a href="#1-3-DataTable-数据驱动" class="headerlink" title="1.3 DataTable 数据驱动"></a>1.3 DataTable 数据驱动</h3><p>所有角色属性通过 DataTable 配置，不写在蓝图里。以英雄数据为例：</p>
<p><img src="/images/portfolio-td/DataTable.jpg" alt="DataTable 结构"></p>
<p>塔的属性表则更进一步——升级相关的字段全部用<strong>数组索引</strong>存储：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 塔的属性——每个字段都是按等级索引的数组</span></span><br><span class="line"><span class="built_in">USTRUCT</span>(BlueprintType)</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">FTowerProperty</span> : <span class="keyword">public</span> FTableRowBase &#123;</span><br><span class="line">    TArray&lt;<span class="type">float</span>&gt; AttackRange;       <span class="comment">// [600, 650, 720, 800]</span></span><br><span class="line">    TArray&lt;int32&gt; DamageMin;         <span class="comment">// [20, 28, 38, 50]</span></span><br><span class="line">    TArray&lt;int32&gt; DamageMax;         <span class="comment">// [30, 40, 55, 70]</span></span><br><span class="line">    TArray&lt;int32&gt; Cost;              <span class="comment">// [100, 150, 220, 0]  // 0 = 满级</span></span><br><span class="line">    TArray&lt;UStaticMesh*&gt; StaticMeshes;  <span class="comment">// 每级不同外观</span></span><br><span class="line">    int32 MaxLevel;                  <span class="comment">// 动态配置等级上限</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>设计意图：新增塔类型只需在 DataTable 加一行，蓝图和代码零改动。</p>
<hr>
<h2 id="二、核心系统：塔的攻击流程"><a href="#二、核心系统：塔的攻击流程" class="headerlink" title="二、核心系统：塔的攻击流程"></a>二、核心系统：塔的攻击流程</h2><p>这是项目中最复杂、最完整的蓝图链路。完整流程分三层：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Tick 层      →  每帧检查攻击条件</span><br><span class="line">探测层       →  Sphere Trace 找到范围内的敌人</span><br><span class="line">攻击执行层   →  随机伤害 + 生成投射物</span><br></pre></td></tr></table></figure>

<h3 id="2-1-定时探测（Timer-Tick-双线程）"><a href="#2-1-定时探测（Timer-Tick-双线程）" class="headerlink" title="2.1 定时探测（Timer + Tick 双线程）"></a>2.1 定时探测（Timer + Tick 双线程）</h3><p><img src="/images/portfolio-td/TowerTick.jpg" alt="TowerTick——定时器与攻击条件"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Event BeginPlay → Set Timer by Function Name:</span><br><span class="line">    Function: &quot;Detect&quot;</span><br><span class="line">    Time: 0.5s, Looping: true</span><br><span class="line"></span><br><span class="line">Event Tick:</span><br><span class="line">    IS NOT EMPTY(DetectedEnemies) AND CanAttack?</span><br><span class="line">      True → Cast To AttackTower → Attack → Set CanAttack = false</span><br></pre></td></tr></table></figure>

<p>设计考量：</p>
<ul>
<li><strong>0.5s 定时探测</strong>而非每帧 Trace——减少物理查询开销，塔防对实时精度要求不高</li>
<li><strong>CanAttack bool 锁</strong>：防止同一帧内多次触发攻击，配合 <code>AttackCooldownTime</code> 在攻击完成后重置</li>
</ul>
<h3 id="2-2-Sphere-Trace-范围检测"><a href="#2-2-Sphere-Trace-范围检测" class="headerlink" title="2.2 Sphere Trace 范围检测"></a>2.2 Sphere Trace 范围检测</h3><p><img src="/images/portfolio-td/Detection1.jpg" alt="Detection——多球体追踪"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Detect Event:</span><br><span class="line">    Get Actor Location (self) → Start &amp; End (同一点)</span><br><span class="line">    Get Attack Range → Radius</span><br><span class="line">    Make Array(Object Type: &quot;Enemy&quot;)</span><br><span class="line">    Multi Sphere Trace For Objects:</span><br><span class="line">        Ignore Self: true</span><br><span class="line">    Out Hits → SET DetectedEnemies</span><br></pre></td></tr></table></figure>

<p>使用 <code>Multi Sphere Trace For Objects</code> 而非 <code>GetAllActorsOfClass</code>：</p>
<ul>
<li><strong>性能</strong>：物理查询比遍历 Actor 列表快，尤其在敌人数量多时</li>
<li><strong>灵活性</strong>：<code>Object Type</code> 过滤 + <code>Ignore Self</code>，后续可扩展为锥形&#x2F;扇形</li>
</ul>
<p>实际用 C++ 封装了命中后的有效性验证：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">ABaseTower::IsEnemyAttackable</span><span class="params">(ABaseEnemy* Enemy)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (Enemy &amp;&amp; !Enemy-&gt;<span class="built_in">IsDead</span>() &amp;&amp;</span><br><span class="line">        (<span class="built_in">GetAttackRange</span>() &gt;= (Enemy-&gt;<span class="built_in">GetActorLocation</span>() - <span class="built_in">GetActorLocation</span>()).<span class="built_in">Length</span>()))</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-3-攻击执行：随机伤害-投射物生成"><a href="#2-3-攻击执行：随机伤害-投射物生成" class="headerlink" title="2.3 攻击执行：随机伤害 + 投射物生成"></a>2.3 攻击执行：随机伤害 + 投射物生成</h3><p><img src="/images/portfolio-td/Attack1.jpg" alt="Attack——Spawn Bullet"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">AttackSingleEnemy:</span><br><span class="line">    Branch(Condition) → True:</span><br><span class="line">        Get World Location(FirePoint)</span><br><span class="line">        Get Closed Enemy(DetectedEnemies) → Enemy</span><br><span class="line">        Get Projectile Speed → Speed</span><br><span class="line">        Random Damage → Damage</span><br><span class="line">        SpawnActor(Bullet Class, Transform, Owner, Target, Speed, Damage)</span><br></pre></td></tr></table></figure>

<p>C++ 中的随机伤害与护甲减伤：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 随机伤害（在范围内取随机值）</span></span><br><span class="line"><span class="function">int32 <span class="title">ABaseCharacter::RandomDamage</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> FMath::<span class="built_in">RandRange</span>(<span class="built_in">GetDamageMin</span>(), <span class="built_in">GetDamageMax</span>());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 核心伤害公式</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">ABaseCharacter::LoseHealth</span><span class="params">(int32 Damage, EDamageType DamageType)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (bIsDead) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">switch</span> (DamageType) &#123;</span><br><span class="line">        <span class="keyword">case</span> Physical:</span><br><span class="line">            Health -= Damage * (<span class="number">1</span> - CharacterData.PhysicalArmor * <span class="number">0.15f</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> Magical:</span><br><span class="line">            Health -= Damage * (<span class="number">1</span> - CharacterData.MagicalArmor);  <span class="comment">// 魔抗全效率</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    OnHealthChange.<span class="built_in">Broadcast</span>();  <span class="comment">// 通知 UI 更新血条</span></span><br><span class="line">    <span class="keyword">if</span> (Health &lt;= <span class="number">0</span>) &#123; bIsDead = <span class="literal">true</span>; <span class="built_in">Dead</span>(); &#125;</span><br><span class="line">    <span class="keyword">return</span> Health &lt;= <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>物抗每点减 15% × 护甲值点数的物理伤害，魔抗每点减 100% × 护甲值点数——<strong>物理护甲比魔抗”软”</strong>，这引导玩家对不同敌人选择不同塔类型。</p>
<hr>
<h2 id="三、敌人系统：Spline-Behavior-Tree"><a href="#三、敌人系统：Spline-Behavior-Tree" class="headerlink" title="三、敌人系统：Spline + Behavior Tree"></a>三、敌人系统：Spline + Behavior Tree</h2><p><img src="/images/portfolio-td/EnemyBase.jpg" alt="EnemyBase——攻击动画与状态处理"></p>
<p>敌人通过 Behavior Tree 驱动三件事：</p>
<table>
<thead>
<tr>
<th>Task</th>
<th>实现方式</th>
<th>关键点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>沿路径移动</strong></td>
<td>Spline + <code>BTTask_GetNextPoint</code></td>
<td>从 DataTable 读取移动速度</td>
</tr>
<tr>
<td><strong>攻击终点</strong></td>
<td><code>BTTask_EnemyAttack</code></td>
<td>调用 <code>OnAttackAnimNotify</code> → Play Montage → 伤害事件</td>
</tr>
<tr>
<td><strong>被击杀</strong></td>
<td><code>OnTakeDamage</code> → 血量检查 → <code>Dead()</code></td>
<td><code>BlueprintImplementableEvent</code>，蓝图中播死亡动画 + 延迟销毁</td>
</tr>
</tbody></table>
<p>Spline 路径还有一个有趣的应用——<strong>投射物贝塞尔曲线</strong>。塔生成的子弹不是直线飞行，而是沿 Bezier 曲线移动，增强了视觉表现力。</p>
<hr>
<h2 id="四、Buff-子系统：WorldSubsystem-工厂模式"><a href="#四、Buff-子系统：WorldSubsystem-工厂模式" class="headerlink" title="四、Buff 子系统：WorldSubsystem + 工厂模式"></a>四、Buff 子系统：WorldSubsystem + 工厂模式</h2><p>这是项目中 C++ 架构最完整的一个系统：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">UMyBuffSubsystem (UWorldSubsystem)</span><br><span class="line">  └── CreateBuff(EBuffType, Rate, Duration) → UBuff*</span><br><span class="line">        ├── USpeedBuff::Attach()     → MaxWalkSpeed *= Rate</span><br><span class="line">        ├── UAttackSpeedBuff::Attach() → AttackCoolDownTime *= Rate</span><br><span class="line">        └── UShieldBuff::Attach()    → PhysicalArmor/MagicalArmor *= Rate</span><br></pre></td></tr></table></figure>

<p>核心设计：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 基类——Timer 管理生命周期</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UBuff::Attach</span><span class="params">(ABaseCharacter* C)</span> </span>&#123;</span><br><span class="line">    Character = C;</span><br><span class="line">    Character-&gt;<span class="built_in">AttachBuff</span>(<span class="keyword">this</span>);</span><br><span class="line">    <span class="built_in">GetWorld</span>()-&gt;<span class="built_in">GetTimerManager</span>().<span class="built_in">SetTimer</span>(Timer, <span class="keyword">this</span>, &amp;UBuff::Detach, Duration);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UBuff::Detach</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Character-&gt;<span class="built_in">RemoveBuff</span>(<span class="keyword">this</span>);</span><br><span class="line">    <span class="built_in">GetWorld</span>()-&gt;<span class="built_in">GetTimerManager</span>().<span class="built_in">ClearTimer</span>(Timer);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子类只覆写效果数值变更</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">USpeedBuff::Attach</span><span class="params">(ABaseCharacter* C)</span> </span>&#123;</span><br><span class="line">    Super::<span class="built_in">Attach</span>(C);</span><br><span class="line">    C-&gt;<span class="built_in">GetCharacterMovement</span>()-&gt;MaxWalkSpeed *= Rate;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">USpeedBuff::Detach</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Super::<span class="built_in">Detach</span>();</span><br><span class="line">    Character-&gt;<span class="built_in">GetCharacterMovement</span>()-&gt;MaxWalkSpeed /= Rate;  <span class="comment">// 恢复</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>选 <code>UWorldSubsystem</code> 而非 Component 的原因：Buff 是跨对象的全局能力——Hero 技能给全体敌人上减速、塔的攻击触发护盾——放在 World 级别自然符合其作用域。</p>
<hr>
<h2 id="五、UI-与交互"><a href="#五、UI-与交互" class="headerlink" title="五、UI 与交互"></a>五、UI 与交互</h2><p><img src="/images/portfolio-td/BuildPanel.jpg" alt="建造面板按钮点击"></p>
<p>整个 UI 分两层：</p>
<table>
<thead>
<tr>
<th>层</th>
<th>主要 Widget</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td><strong>局内</strong></td>
<td><code>WBP_GameHUD</code>, <code>WBP_BuildPanel</code>, <code>WBP_HealthBar_Info</code>, <code>WBP_SkillPanel</code>, <code>WBP_NextWaveBTN</code></td>
<td>实时血量、防御塔建造、技能释放、波次推进</td>
</tr>
<tr>
<td><strong>局外</strong></td>
<td><code>WBP_StartPanel</code>, <code>WBP_MenuPanel</code>, <code>WBP_HeroPanel</code>, <code>WBP_TowerPanel1</code>, <code>WBP_LoadPanel</code></td>
<td>关卡选择、英雄&#x2F;塔图鉴、存档加载</td>
</tr>
</tbody></table>
<p>UI 数据绑定流程：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GameState(金币变化) → OnHealthChange Delegate → WBP_GameStatePanel(更新文字)</span><br><span class="line">Character(血量变化) → OnHealthChange Delegate → WBP_HealthBar_Info(ProgressBar)</span><br></pre></td></tr></table></figure>

<p>C++ 中声明的事件委托：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">DECLARE_DYNAMIC_MULTICAST_DELEGATE</span>(FOnHealthChangedDelegate);</span><br><span class="line"></span><br><span class="line"><span class="built_in">UPROPERTY</span>(BlueprintAssignable)</span><br><span class="line">FOnHealthChangedDelegate OnHealthChange;</span><br></pre></td></tr></table></figure>

<p>所有 Widget 通过 <code>Event Construct</code> → <code>Get Game Instance</code> → <code>Cast</code> → 获取数据 → <code>ForEachLoop</code> 遍历子控件来初始化，构成一个从 GameState → HUD → 子控件的单向数据流。</p>
<hr>
<h2 id="六、波次管理系统"><a href="#六、波次管理系统" class="headerlink" title="六、波次管理系统"></a>六、波次管理系统</h2><p><img src="/images/portfolio-td/WaveSystem.jpg" alt="波次切换流程"></p>
<p>波次数据同样走 DataTable：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">USTRUCT</span>()</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">FEnemyWave</span> : <span class="keyword">public</span> FTableRowBase &#123;</span><br><span class="line">    TArray&lt;FEnemyNumTime&gt; WaveEnemies;   <span class="comment">// 本波敌人列表（名称+数量+间隔+路线）</span></span><br><span class="line">    int32 WaveCoolDownTime;              <span class="comment">// 波间冷却</span></span><br><span class="line">    FString WaveEvent;                   <span class="comment">// 波次事件描述</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">USTRUCT</span>()</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">FGameData</span> : <span class="keyword">public</span> FTableRowBase &#123;</span><br><span class="line">    TArray&lt;FEnemyWave&gt; Waves;            <span class="comment">// 全部波次</span></span><br><span class="line">    int32 Coins, Health;                 <span class="comment">// 起始金币、生命</span></span><br><span class="line">    TSoftObjectPtr&lt;UWorld&gt; Level;        <span class="comment">// 关卡地图</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>波次调度流程：<code>OnGameStart</code> → 读 DT_GameData → <code>CreateEnemySpawner</code> → 按 <code>CoolDownTime</code> 间隔生成 → 最后一波 &amp; 敌人清空 → 胜利。</p>
<p>GameMode 每 Tick 检查胜利条件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Event Tick:</span><br><span class="line">    Is Last Wave? AND Generated Enemies IS EMPTY?</span><br><span class="line">        True → Victory → Open WinPanel</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="七、踩坑记录"><a href="#七、踩坑记录" class="headerlink" title="七、踩坑记录"></a>七、踩坑记录</h2><h3 id="1-Behavior-Tree-Task-Finish-必须手动标记-Success"><a href="#1-Behavior-Tree-Task-Finish-必须手动标记-Success" class="headerlink" title="1. Behavior Tree Task Finish 必须手动标记 Success"></a>1. Behavior Tree Task Finish 必须手动标记 Success</h3><p><strong>现象</strong>：AI 走到第一个路径点后停止不动。</p>
<p><strong>原因</strong>：<code>BTTask_GetNextPoint</code> 的 <code>Finish Execute</code> 节点没有勾选 <code>Success</code>，导致 BT 认为 Task 仍在运行中。这是 UE Behavior Tree 最常见的坑——Task 不返回 Success，后续节点永远不会执行。</p>
<p><strong>Git 记录</strong>：</p>
<blockquote>
<p><code>paramove to spline and random spawn point, fix ai malfunction (in task finish, success unchecked)</code></p>
</blockquote>
<h3 id="2-碰撞预设导致的检测失效"><a href="#2-碰撞预设导致的检测失效" class="headerlink" title="2. 碰撞预设导致的检测失效"></a>2. 碰撞预设导致的检测失效</h3><p><strong>现象</strong>：<code>Multi Sphere Trace For Objects</code> 永远返回空数组。</p>
<p><strong>原因</strong>：敌人角色的 Collision Preset 中 <code>Object Type</code> 不是 “Enemy” 通道。在 Project Settings 创建了自定义 Object Channel 后，忘记在 Enemy 蓝图上设置 Collision Preset。</p>
<p><strong>教训</strong>：自定义物理通道需要在每个相关 Actor 上确认 Collision 设置，不能只在代码中写过滤逻辑就假设能工作。</p>
<h3 id="3-移动端构建错误"><a href="#3-移动端构建错误" class="headerlink" title="3. 移动端构建错误"></a>3. 移动端构建错误</h3><p><strong>现象</strong>：<code>Makefile error</code>，项目无法编译。</p>
<p><strong>原因</strong>：引擎版本从 5.5 升级到 5.6 后，<code>.uproject</code> 中残留了一个已禁用的插件引用（<code>&quot;Cargo&quot;: {&quot;Enabled&quot;: false}</code>），UBT 在处理时产生兼容性问题。</p>
<p><strong>解决</strong>：清理 <code>.uproject</code> 中无效的插件声明即可。</p>
<hr>
<h2 id="八、架构可改进点"><a href="#八、架构可改进点" class="headerlink" title="八、架构可改进点"></a>八、架构可改进点</h2><table>
<thead>
<tr>
<th>问题</th>
<th>当前做法</th>
<th>改进方向</th>
</tr>
</thead>
<tbody><tr>
<td><strong>等级数组越界</strong></td>
<td><code>TowerData.DamageMin[Level-1]</code> 无边界检查</td>
<td>封装 <code>GetValueAtLevel()</code> 加 <code>check()</code> 断言</td>
</tr>
<tr>
<td><strong>BaseCharacter 不必要地继承 ACharacter</strong></td>
<td>所有角色继承 <code>ACharacter</code>，塔防不需要 SkeletalMesh 和物理</td>
<td>轻量级角色可改为继承 <code>APawn</code> 或自定义 <code>UObject</code></td>
</tr>
<tr>
<td><strong>Buff 直接修改 CharacterData</strong></td>
<td><code>AttackSpeedBuff</code> 直接改了 <code>CharacterData.AttackCoolDownTime</code>，如果多个 Buff 叠加可能出错</td>
<td>引入属性快照机制，Buff 修改前保存原始值</td>
</tr>
<tr>
<td><strong>缺少网络同步</strong></td>
<td>单机项目，未考虑 RPC 和属性复制</td>
<td>如需多人模式，需要在 GameMode 加 Server RPC，GameState 加 Replicated 属性</td>
</tr>
<tr>
<td><strong>波次系统耦合在 GameMode 蓝图中</strong></td>
<td>波次逻辑、敌人创建、UI 更新全部在一个蓝图里</td>
<td>拆分为独立的 <code>WaveManager</code> Component</td>
</tr>
<tr>
<td><strong>DataTable Init 传原始指针</strong></td>
<td><code>Init(FName, UDataTable*)</code> 无空指针检查</td>
<td>用 <code>TObjectPtr&lt;UDataTable&gt;</code> + <code>ensure()</code></td>
</tr>
</tbody></table>
<hr>
<blockquote>
<p>这是我学习 UE 过程中的第一个完整项目，很多设计决策是在边学边做的过程中形成的。如果对塔防、蓝图架构或 UE 任何方面有讨论兴趣，欢迎交流。</p>
</blockquote>
]]></content>
      <categories>
        <category>Portfolio</category>
      </categories>
      <tags>
        <tag>Unreal Engine</tag>
        <tag>Blueprint</tag>
        <tag>UE5</tag>
        <tag>Tower Defense</tag>
        <tag>Gameplay Programming</tag>
        <tag>DataTable</tag>
        <tag>塔防</tag>
      </tags>
  </entry>
  <entry>
    <title>UE5 动画系统两种架构：TPS Blend Space 状态机 vs ARPG Montage + GAS 事件驱动</title>
    <url>/2026/08/06/UE5%E5%8A%A8%E7%94%BB%E7%B3%BB%E7%BB%9F%E4%B8%A4%E7%A7%8D%E6%9E%B6%E6%9E%84-TPS%E4%B8%8EARPG%E5%AF%B9%E6%AF%94/</url>
    <content><![CDATA[<h2 id="项目概览"><a href="#项目概览" class="headerlink" title="项目概览"></a>项目概览</h2><p>这两篇文章的上下文——一个侧重动画系统本身的学习，一个侧重 GAS 战斗系统的搭建。但放在一起看，它们的动画架构恰好代表了 UE5 的两条主流路线：</p>
<table>
<thead>
<tr>
<th></th>
<th>TPS Tutorial</th>
<th>Crunch</th>
</tr>
</thead>
<tbody><tr>
<td><strong>引擎版本</strong></td>
<td>5.3</td>
<td>5.4</td>
</tr>
<tr>
<td><strong>类型</strong></td>
<td>第三人称射击</td>
<td>近战 ARPG &#x2F; MOBA</td>
</tr>
<tr>
<td><strong>角色资源</strong></td>
<td>Marketplace 侦探模型 + IK Retarget</td>
<td>Paragon Crunch</td>
</tr>
<tr>
<td><strong>动画资产规模</strong></td>
<td>~200+ 序列帧</td>
<td>~20 序列帧</td>
</tr>
<tr>
<td><strong>Blend Space</strong></td>
<td>7 个</td>
<td>2 个</td>
</tr>
<tr>
<td><strong>Animation Montage</strong></td>
<td>4 个</td>
<td>4 个（按技能组织）</td>
</tr>
<tr>
<td><strong>C++ AnimInstance</strong></td>
<td>无</td>
<td>自定义 <code>UCAnimInstance</code></td>
</tr>
<tr>
<td><strong>自定义 AnimNotify</strong></td>
<td>无</td>
<td><code>AN_SendGameplayEvent</code> + <code>AN_SendTargetGroup</code></td>
</tr>
<tr>
<td><strong>核心驱动</strong></td>
<td>Enhanced Input → CharacterMovement → AnimBP</td>
<td>GAS AbilityTask → Montage → GameplayEvent</td>
</tr>
</tbody></table>
<hr>
<h2 id="一、核心差异：表现丰富度-vs-通信精度"><a href="#一、核心差异：表现丰富度-vs-通信精度" class="headerlink" title="一、核心差异：表现丰富度 vs 通信精度"></a>一、核心差异：表现丰富度 vs 通信精度</h2><p>两个项目的设计目标决定了它们走向了不同的动画架构：</p>
<ul>
<li><p><strong>TPS Tutorial</strong> 的目标是”学会 UE5 动画系统能做什么”——所以它把 Blend Space、状态机、AimOffset、Montage 全部搭了一遍。结果是<strong>动画表现极其丰富</strong>（八向移动 × 多种姿态 × 过渡动画），但 Gameplay 通信层很薄（一个 LineTrace 完事）。</p>
</li>
<li><p><strong>Crunch</strong> 的目标是”做出能联网的近战战斗”——所以它的动画资产很少（Paragon 自带），但 <strong>Gameplay 通信层极其精密</strong>：自定义 AnimNotify 在特定帧做 SphereTrace → 打包 TargetData → 发 GameplayEvent → GAS Ability 收到后施加 GE。</p>
</li>
</ul>
<p>一句话概括：<strong>TPS 的动画系统是”展示型”的，Crunch 的动画系统是”功能型”的。</strong></p>
<hr>
<h2 id="二、同一功能，两种实现"><a href="#二、同一功能，两种实现" class="headerlink" title="二、同一功能，两种实现"></a>二、同一功能，两种实现</h2><h3 id="2-1-方向计算"><a href="#2-1-方向计算" class="headerlink" title="2.1 方向计算"></a>2.1 方向计算</h3><p><strong>TPS — AnimBP Event Graph 内置函数：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在蓝图里一行搞定</span></span><br><span class="line">Direction = <span class="built_in">CalculateDirection</span>(Velocity, ActorRotation);  <span class="comment">// 返回 -180~180</span></span><br><span class="line">Speed = Velocity.<span class="built_in">Size</span>();</span><br></pre></td></tr></table></figure>

<p><strong>Crunch — C++ 手动分解前后&#x2F;左右分量：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">UCAnimInstance::NativeUpdateAnimation</span><span class="params">(<span class="type">float</span> DeltaSeconds)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    FVector Velocity = OwnerCharacter-&gt;<span class="built_in">GetVelocity</span>();</span><br><span class="line">    FRotator ControlRot = OwnerCharacter-&gt;<span class="built_in">GetBaseAimRotation</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 分解为前后和左右两个独立分量</span></span><br><span class="line">    FwdSpeed   = Velocity.<span class="built_in">Dot</span>(ControlRot.<span class="built_in">Vector</span>());</span><br><span class="line">    RightSpeed = -Velocity.<span class="built_in">Dot</span>(ControlRot.<span class="built_in">Vector</span>().<span class="built_in">Cross</span>(FVector::UpVector));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 转向速度 + 平滑</span></span><br><span class="line">    FRotator BodyRotDelta = <span class="built_in">NormalizedDeltaRotator</span>(BodyRot, BodyPreviousRotation);</span><br><span class="line">    YawSpeed = BodyRotDelta.Yaw / DeltaSeconds;</span><br><span class="line">    SmoothedYawSpeed = <span class="built_in">FInterpTo</span>(SmoothedYawSpeed, YawSpeed, DeltaSeconds, <span class="number">1.f</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 瞄准偏移</span></span><br><span class="line">    LookRotOffset = <span class="built_in">NormalizedDeltaRotator</span>(ControlRot, BodyRot);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>维度</th>
<th>TPS</th>
<th>Crunch</th>
</tr>
</thead>
<tbody><tr>
<td>方向表示</td>
<td>单个 Direction 角度</td>
<td>FwdSpeed + RightSpeed 双分量</td>
</tr>
<tr>
<td>适用场景</td>
<td>前进后退对称的速度模型</td>
<td>前进后退不对称（后退速度上限更低）</td>
</tr>
<tr>
<td>转向处理</td>
<td>无</td>
<td>计算 YawSpeed + 平滑，驱动转身动画</td>
</tr>
<tr>
<td>AimOffset</td>
<td>在 AnimBP 中用 BlendSpace 处理</td>
<td>C++ 中预计算 LookRotOffset，传入 BlendSpace</td>
</tr>
</tbody></table>
<p>Crunch 的双分量设计更灵活：前进速度 600、后退速度 200 时，<code>FwdSpeed</code> 可以是负数（后退），而 <code>CalculateDirection</code> 只输出角度，丢失了”你在后退且速度受限”这个信息。</p>
<h3 id="2-2-Animation-Montage-生命周期管理"><a href="#2-2-Animation-Montage-生命周期管理" class="headerlink" title="2.2 Animation Montage 生命周期管理"></a>2.2 Animation Montage 生命周期管理</h3><p>这是两个项目差距最大的设计决策。</p>
<p><strong>TPS — 隐式管理，分两层：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">玩家按下左键</span><br><span class="line">  → C++ MyCharacter::Fire()</span><br><span class="line">    → 如果是第一次射击 → ICRRecoilInterface::StartShooting()</span><br><span class="line">    → BaseGun::GunFire()</span><br><span class="line">      → ShootOnce()            // LineTrace + 伤害</span><br><span class="line">      → PlayFireAnimation()    // 枪械自身 SkeletalMesh 动画</span><br><span class="line">      → CameraShake</span><br><span class="line">  → (同时) AnimBP 蓝图侧监听 &quot;正在开火&quot; 状态 → PlayMontage(AM_Fire)</span><br></pre></td></tr></table></figure>

<p>Montage 的播放不是从 C++ 显式触发的，而是 AnimBP 蓝图中通过”检测到正在开火 → 播 AM_Fire”的间接逻辑。FP 枪械的动画和角色 Montage 在两条独立的轨道上，同步靠蓝图端的 Tick 判断。</p>
<p><strong>Crunch — 显式管理，全在一个类内：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">UGA_Combo::ActivateAbility</span><span class="params">(...)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// ① Montage 播放由 AbilityTask 管理，不是手动 PlayMontage</span></span><br><span class="line">    UAbilityTask_PlayMontageAndWait* PlayTask =</span><br><span class="line">        UAbilityTask_PlayMontageAndWait::<span class="built_in">CreatePlayMontageAndWaitProxy</span>(</span><br><span class="line">            <span class="keyword">this</span>, NAME_None, ComboMontage);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ② 四个退出路径全部绑定 EndAbility —— Montage 结束 = 技能结束</span></span><br><span class="line">    PlayTask-&gt;OnBlendOut.<span class="built_in">AddDynamic</span>(<span class="keyword">this</span>, &amp;UGA_Combo::K2_EndAbility);</span><br><span class="line">    PlayTask-&gt;OnCancelled.<span class="built_in">AddDynamic</span>(<span class="keyword">this</span>, &amp;UGA_Combo::K2_EndAbility);</span><br><span class="line">    PlayTask-&gt;OnCompleted.<span class="built_in">AddDynamic</span>(<span class="keyword">this</span>, &amp;UGA_Combo::K2_EndAbility);</span><br><span class="line">    PlayTask-&gt;OnInterrupted.<span class="built_in">AddDynamic</span>(<span class="keyword">this</span>, &amp;UGA_Combo::K2_EndAbility);</span><br><span class="line">    PlayTask-&gt;<span class="built_in">ReadyForActivation</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ③ 同时等待 AnimNotify 发来的 GameplayEvent</span></span><br><span class="line">    UAbilityTask_WaitGameplayEvent* WaitTask =</span><br><span class="line">        UAbilityTask_WaitGameplayEvent::<span class="built_in">WaitGameplayEvent</span>(</span><br><span class="line">            <span class="keyword">this</span>, <span class="built_in">GetComboChangedEventTag</span>(), ...);</span><br><span class="line">    WaitTask-&gt;EventReceived.<span class="built_in">AddDynamic</span>(<span class="keyword">this</span>, &amp;UGA_Combo::ComboChangedEventReceived);</span><br><span class="line">    WaitTask-&gt;<span class="built_in">ReadyForActivation</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ④ 等待玩家下一段按键输入</span></span><br><span class="line">    <span class="built_in">SetupWaitComboInputPress</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Combo 段位切换：在动画中通过 AnimNotify 触发</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UGA_Combo::ComboChangedEventReceived</span><span class="params">(FGameplayEventData Data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    NextComboName = TagNames.<span class="built_in">Last</span>();      <span class="comment">// &quot;Combo1&quot; / &quot;Combo2&quot; / &quot;Combo3&quot;</span></span><br><span class="line">    <span class="built_in">SetupWaitComboInputPress</span>();           <span class="comment">// 继续等待输入 → HandleInputPressed → TryCommitCombo</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 下一段 Combo：用 Montage_SetNextSection 平滑过渡</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UGA_Combo::TryCommitCombo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    UAnimInstance* OwnerAnimInst = <span class="built_in">GetOwnerAnimInstance</span>();</span><br><span class="line">    OwnerAnimInst-&gt;<span class="built_in">Montage_SetNextSection</span>(</span><br><span class="line">        OwnerAnimInst-&gt;<span class="built_in">Montage_GetCurrentSection</span>(ComboMontage),</span><br><span class="line">        NextComboName, ComboMontage);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>维度</th>
<th>TPS</th>
<th>Crunch</th>
</tr>
</thead>
<tbody><tr>
<td>Montage 触发</td>
<td>AnimBP 蓝图间接触发</td>
<td>C++ GAS AbilityTask 显式管理</td>
</tr>
<tr>
<td>结束回调</td>
<td>依赖 AnimBP 状态机回退</td>
<td><code>OnBlendOut</code> &#x2F; <code>OnCancelled</code> &#x2F; <code>OnInterrupted</code> 全部绑定</td>
</tr>
<tr>
<td>技能-动画关系</td>
<td>松散耦合（各管各）</td>
<td>强内聚（一个 GA 对象管理一切）</td>
</tr>
<tr>
<td>Combo 段位切换</td>
<td>无</td>
<td><code>Montage_SetNextSection</code> 无缝衔接</td>
</tr>
<tr>
<td>可调试性</td>
<td>需要在 AnimBP 和 C++ 之间跳转</td>
<td>全部在一个 <code>.cpp</code> 文件中</td>
</tr>
</tbody></table>
<p>Crunch 方式的核心优势：<strong>一个 Combo 技能的全部逻辑（动画播放 → 等待输入 → AnimNotify 事件 → 段位切换 → 伤害施加 → 技能结束）都在一个 <code>UGA_Combo</code> 类里</strong>。审阅或调试时不需要在 AnimBP 蓝图和 C++ 之间来回跳。</p>
<h3 id="2-3-伤害判定时机"><a href="#2-3-伤害判定时机" class="headerlink" title="2.3 伤害判定时机"></a>2.3 伤害判定时机</h3><p>这是被 Gameplay 类型决定的差异——不是”谁做得更好”，而是”谁更适合自己做的事”。</p>
<p><strong>TPS — 即时命中（Hitscan）：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">ABaseGun::ShootOnce</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 按下扳机的同一帧 → LineTrace → 伤害</span></span><br><span class="line">    FVector StartLoc = CameraManager-&gt;<span class="built_in">GetCameraLocation</span>();</span><br><span class="line">    FVector EndLoc = StartLoc + SpreadRotator.<span class="built_in">RotateVector</span>(</span><br><span class="line">        CameraManager-&gt;<span class="built_in">GetActorForwardVector</span>()) * <span class="number">2000</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">bool</span> bHit = <span class="built_in">GetWorld</span>()-&gt;<span class="built_in">LineTraceSingleByChannel</span>(</span><br><span class="line">        Hit, StartLoc, EndLoc, ECC_Visibility, Params);</span><br><span class="line">    <span class="keyword">if</span> (bHit)</span><br><span class="line">        MyCharacter-&gt;<span class="built_in">OnGunHit</span>(Hit, <span class="keyword">this</span>);  <span class="comment">// → 蓝图事件</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Crunch — 动画帧判定（Frame-accurate）：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// AN_SendTargetGroup::Notify() — 挂在 ComboMontage 的&quot;拳头命中帧&quot;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UAN_SendTargetGroup::Notify</span><span class="params">(...)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 在动画特定帧做 SphereTrace</span></span><br><span class="line">    UKismetSystemLibrary::<span class="built_in">SphereTraceMultiForObjects</span>(</span><br><span class="line">        MeshComp, StartLoc, EndLoc, SphereSweepRadius, ...);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 打包命中结果 → TargetData → GameplayEvent → GAS</span></span><br><span class="line">    FGameplayAbilityTargetData_SingleTargetHit* TargetHit =</span><br><span class="line">        <span class="keyword">new</span> <span class="built_in">FGameplayAbilityTargetData_SingleTargetHit</span>(HitResult);</span><br><span class="line">    Data.TargetData.<span class="built_in">Add</span>(TargetHit);</span><br><span class="line"></span><br><span class="line">    UAbilitySystemBlueprintLibrary::<span class="built_in">SendGameplayEventToActor</span>(</span><br><span class="line">        MeshComp-&gt;<span class="built_in">GetOwner</span>(), EventTag, Data);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// GA_Combo::DoDamage() — 收到事件后施加伤害</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UGA_Combo::DoDamage</span><span class="params">(FGameplayEventData Data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; HitResultCount; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        FHitResult Hit = <span class="built_in">GetHitResultFromTargetData</span>(Data.TargetData, i);</span><br><span class="line">        <span class="built_in">ApplyGameplayEffectToHitResultActor</span>(Hit, <span class="built_in">GetDamageEffectForCurrentCombo</span>(), ...);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>维度</th>
<th>TPS</th>
<th>Crunch</th>
</tr>
</thead>
<tbody><tr>
<td>判定时机</td>
<td>输入帧（按下扳机那一刻）</td>
<td>动画帧（拳头到达最远点的那一帧）</td>
</tr>
<tr>
<td>判定方式</td>
<td>LineTrace（射线）</td>
<td>SphereTrace（球体扫描）</td>
</tr>
<tr>
<td>适合类型</td>
<td>射击——子弹瞬间到达</td>
<td>近战——武器轨迹扫过敌人</td>
</tr>
<tr>
<td>伤害可变性</td>
<td>固定（枪的伤害值）</td>
<td>动态（Combo 段位不同 → GE 不同）</td>
</tr>
<tr>
<td>判定失败的影响</td>
<td>打不中（手感差）</td>
<td>视觉上打中了但没伤害（手感更差）</td>
</tr>
</tbody></table>
<p>TPS 的 LineTrace 在开枪那一帧执行——开火动画刚起手，子弹已经判定完毕。这对射击游戏来说是正常体验，但如果你把枪口火焰动画和实际 Trace 时间对齐，会发现 Trace 比枪口火花早了 2-3 帧。Crunch 的 AnimNotify 方案把 Trace 精确放在动画帧上——如果是近战游戏，这个精确度直接影响手感。</p>
<h3 id="2-4-Gameplay-状态-→-动画状态的数据通道"><a href="#2-4-Gameplay-状态-→-动画状态的数据通道" class="headerlink" title="2.4 Gameplay 状态 → 动画状态的数据通道"></a>2.4 Gameplay 状态 → 动画状态的数据通道</h3><p><strong>TPS</strong> 使用最简单直接的路径：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CharacterMovement (bIsCrouching, IsFalling)</span><br><span class="line">    → AnimBP Event Graph 直接读取</span><br><span class="line">    → 驱动 State Machine 转换</span><br></pre></td></tr></table></figure>

<p><strong>Crunch</strong> 使用 GameplayTag 作为中间层：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 初始化时注册 GameplayTag 监听</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UCAnimInstance::NativeInitializeAnimation</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    UAbilitySystemComponent* OwnerASC =</span><br><span class="line">        UAbilitySystemBlueprintLibrary::<span class="built_in">GetAbilitySystemComponent</span>(<span class="built_in">TryGetPawnOwner</span>());</span><br><span class="line">    <span class="keyword">if</span> (OwnerASC)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// Aim 状态通过 GameplayTag 同步</span></span><br><span class="line">        OwnerASC-&gt;<span class="built_in">RegisterGameplayTagEvent</span>(</span><br><span class="line">            UCAbilitySystemStatics::<span class="built_in">GetAimStatsTag</span>()</span><br><span class="line">        ).<span class="built_in">AddUObject</span>(<span class="keyword">this</span>, &amp;UCAnimInstance::OwnerAimTagChanged);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Tag 变化 → 动画状态</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UCAnimInstance::OwnerAimTagChanged</span><span class="params">(<span class="type">const</span> FGameplayTag Tag, int32 NewCount)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    bIsAiming = NewCount != <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>为什么 Crunch 需要这一层？</strong></p>
<p>因为 Crunch 的角色状态（瞄准&#x2F;死亡&#x2F;眩晕&#x2F;无敌）不是简单的 <code>bool</code>，而是 GAS 的 <code>FGameplayTag</code> ——一个角色可以同时持有多个 Tag，且这些 Tag 通过网络自动复制。<code>bIsAiming</code> 在客户端和服务端通过同一个机制同步，不需要额外的手动 RPC。</p>
<p>TPS 不需要这层间接——它不做网络同步，<code>bIsCrouching</code> 本身就是 CharacterMovement 的同步属性。</p>
<hr>
<h2 id="三、线程模型：AnimBP-游戏线程-vs-C-工作线程"><a href="#三、线程模型：AnimBP-游戏线程-vs-C-工作线程" class="headerlink" title="三、线程模型：AnimBP 游戏线程 vs C++ 工作线程"></a>三、线程模型：AnimBP 游戏线程 vs C++ 工作线程</h2><p>Crunch 的 <code>CAnimInstance</code> 明确区分了两个更新函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// NativeUpdateAnimation — 游戏线程</span></span><br><span class="line"><span class="comment">// 只做数据收集：读 Velocity、算 Speed、算 Yaw</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">NativeUpdateAnimation</span><span class="params">(<span class="type">float</span> DeltaSeconds)</span> <span class="keyword">override</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// NativeThreadSafeUpdateAnimation — 工作线程</span></span><br><span class="line"><span class="comment">// 在 Anim Graph 更新前执行，可以做混合计算</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">NativeThreadSafeUpdateAnimation</span><span class="params">(<span class="type">float</span> DeltaSeconds)</span> <span class="keyword">override</span></span>;</span><br></pre></td></tr></table></figure>

<p>TPS 因为完全在 AnimBP 的 Event Graph 中做计算，所有 Update 都在游戏线程的 Blueprint 虚拟机上执行——没有利用 UE5 的动画工作线程。</p>
<p><strong>什么时候这个区别重要？</strong></p>
<p>当场景中有 50+ 个角色同时播放动画时，游戏线程的 AnimBP 计算会成为瓶颈。把数据收集放在游戏线程、混合计算放在工作线程，是 UE5 推荐的做法。但对于单机 Demo 来说，这个优化是过早的。</p>
<hr>
<h2 id="四、AnimNotify：TPS-没有，Crunch-有"><a href="#四、AnimNotify：TPS-没有，Crunch-有" class="headerlink" title="四、AnimNotify：TPS 没有，Crunch 有"></a>四、AnimNotify：TPS 没有，Crunch 有</h2><p>TPS 不需要自定义 AnimNotify——它的 Montage 不向 Gameplay 系统发送信息。开火是 C++ 端主动触发的，换弹也是 C++ 端的 Timer 管理，动画只需要”看起来对”。</p>
<p>Crunch 有两个自定义 AnimNotify，它们是动画系统和 GAS 之间的桥梁：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// AN_SendGameplayEvent：在动画特定帧向 ASC 发送 GameplayEvent</span></span><br><span class="line"><span class="comment">// 用于 Combo 段位切换（&quot;Combo1 → Combo2&quot;的切换帧）</span></span><br><span class="line">UAN_SendGameplayEvent::<span class="built_in">Notify</span>(...) &#123;</span><br><span class="line">    UAbilitySystemBlueprintLibrary::<span class="built_in">SendGameplayEventToActor</span>(</span><br><span class="line">        MeshComp-&gt;<span class="built_in">GetOwner</span>(), GameplayEventTag, <span class="built_in">FGameplayEventData</span>());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// AN_SendTargetGroup：在动画特定帧做伤害判定</span></span><br><span class="line"><span class="comment">// 用于 Combo 伤害帧（&quot;拳头命中&quot;那一帧）</span></span><br><span class="line">UAN_SendTargetGroup::<span class="built_in">Notify</span>(...) &#123;</span><br><span class="line">    <span class="comment">// SphereTrace → 筛选敌对阵营 → 发送 GameplayEvent + GameplayCue</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>设计原则：AnimNotify 不做 Gameplay 逻辑，它只负责”在正确的时刻发消息”。</strong> 收到消息后怎么做——伤害多少、施加什么 GE——全部在 GAS Ability 中决定。这意味着同一个 AnimNotify 可以用在不同的技能里，伤害逻辑由各自的 Ability 处理。</p>
<hr>
<h2 id="五、动画资产组织"><a href="#五、动画资产组织" class="headerlink" title="五、动画资产组织"></a>五、动画资产组织</h2><table>
<thead>
<tr>
<th>维度</th>
<th>TPS</th>
<th>Crunch</th>
</tr>
</thead>
<tbody><tr>
<td>移动动画</td>
<td>按状态 × 方向 × 姿态分层：Walk × 8 + Run × 8 + Crouch × 8 + Aim × 8 + Equip × 8</td>
<td>Paragon 自带 Jog&#x2F;Idle，用 Blend Space 混合</td>
</tr>
<tr>
<td>动作动画</td>
<td>按武器类型分：Pistol&#x2F;AR 各一套 Fire&#x2F;Reload</td>
<td>按技能分：Combo &#x2F; UpperCut &#x2F; GroundBlast</td>
</tr>
<tr>
<td>Montage 组织</td>
<td>按动作类型分：<code>AM_Fire</code> &#x2F; <code>AM_Reload</code> &#x2F; <code>AM_Dodge</code> &#x2F; <code>AM_Punch</code></td>
<td>按技能分：<code>AM_Combo</code> &#x2F; <code>AM_UpperCut</code> &#x2F; <code>AM_GroundBlast</code></td>
</tr>
<tr>
<td>过渡动画</td>
<td>非常全：Idle→Walk (8 dir) &#x2F; Idle→Run (8 dir) &#x2F; WalkStop &#x2F; RunStop (L&#x2F;R foot)</td>
<td>依赖 Root Motion</td>
</tr>
<tr>
<td>IK</td>
<td>Control Rig Foot IK + PostProcess ABP</td>
<td>Control Rig Foot IK (<code>CR_Crunch_IKFoot</code>)</td>
</tr>
</tbody></table>
<p>TPS 的动画组织是”从下往上”——从最基本的移动动画开始，一层层叠加（Additive AimOffset → LayeredBlendPerBone 武器姿态 → Montage 插播）。Crunch 是”从上往下”——从技能设计开始（Combo 要有 3 段 → 每段一个 Section → Section 之间用 AnimNotify 桥接），移动动画只是技能的”默认背景层”。</p>
<hr>
<h2 id="六、适用场景与选择建议"><a href="#六、适用场景与选择建议" class="headerlink" title="六、适用场景与选择建议"></a>六、适用场景与选择建议</h2><table>
<thead>
<tr>
<th>场景</th>
<th>推荐方向</th>
<th>原因</th>
</tr>
</thead>
<tbody><tr>
<td>学习动画系统本身</td>
<td><strong>TPS 方式</strong></td>
<td>手搭 Blend Space + 状态机 + Montage 全流程</td>
</tr>
<tr>
<td>射击游戏（Hitscan）</td>
<td><strong>TPS 方式</strong></td>
<td>C++ LineTrace，动画只管表现</td>
</tr>
<tr>
<td>近战动作游戏</td>
<td><strong>Crunch 方式</strong></td>
<td>AnimNotify 帧判定 + GAS 管理技能生命周期</td>
</tr>
<tr>
<td>需要联网多人</td>
<td><strong>Crunch 方式</strong></td>
<td>GAS 的 Tag&#x2F;Attribute 自动复制，AnimNotify 发 Event</td>
</tr>
<tr>
<td>团队以蓝图为主</td>
<td><strong>TPS 方式</strong></td>
<td>AnimBP Event Graph 承担大部分工作</td>
</tr>
<tr>
<td>技能需要复杂逻辑</td>
<td><strong>Crunch 方式</strong></td>
<td>自定义 C++ AnimInstance + AbilityTask 管理一切</td>
</tr>
<tr>
<td>大量 AI 角色同时存在</td>
<td><strong>Crunch 方式</strong></td>
<td>工作线程 Update + 少量 Blend Space → 性能更好</td>
</tr>
</tbody></table>
<hr>
<h2 id="七、为什么两套方案不矛盾"><a href="#七、为什么两套方案不矛盾" class="headerlink" title="七、为什么两套方案不矛盾"></a>七、为什么两套方案不矛盾</h2><p>做实际项目时，你不会只选一种——你会把两者的优势结合起来：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">项目动画系统 = TPS 的 Blend Space 移动层</span><br><span class="line">              + Crunch 的 GAS + AnimNotify 技能层</span><br><span class="line">              + 自定义 C++ AnimInstance 数据层</span><br></pre></td></tr></table></figure>

<p>具体来说：</p>
<ul>
<li><strong>移动系统</strong>用 TPS 的方案：状态机驱动 + 丰富的 Blend Space 过渡</li>
<li><strong>战斗技能</strong>用 Crunch 的方案：GAS AbilityTask 管理 Montage 生命周期 + AnimNotify 发 GameplayEvent 做帧判定</li>
<li><strong>数据层</strong>统一在 C++ AnimInstance 中计算，GameplayTag 监听 Gameplay 状态</li>
</ul>
<p>两个项目合在一起，覆盖了 UE5 角色动画的三大支柱：<strong>Blend Space 状态机</strong>（表现力）、<strong>Montage + AnimNotify 事件系统</strong>（功能性）、<strong>C++ AnimInstance + GameplayTag</strong>（数据通信）。</p>
<hr>
<blockquote>
<p>如果你也在用 UE5 搭建角色动画系统，或者对这两种架构有自己的实践经验，欢迎交流。</p>
</blockquote>
]]></content>
      <categories>
        <category>Portfolio</category>
      </categories>
      <tags>
        <tag>UE5</tag>
        <tag>Unreal</tag>
        <tag>Animation</tag>
        <tag>AnimationBlueprint</tag>
        <tag>BlendSpace</tag>
        <tag>Montage</tag>
        <tag>GAS</tag>
        <tag>GameplayAbilitySystem</tag>
        <tag>Portfolio</tag>
      </tags>
  </entry>
  <entry>
    <title>TPS动画系统实践：Animation Blueprint + 八向移动 + Montage 的实现步骤</title>
    <url>/2026/08/06/TPS%E5%8A%A8%E7%94%BB%E7%B3%BB%E7%BB%9F%E5%AE%9E%E8%B7%B5-ABP-%E5%85%AB%E5%90%91%E7%A7%BB%E5%8A%A8-Montage/</url>
    <content><![CDATA[<h2 id="项目概览"><a href="#项目概览" class="headerlink" title="项目概览"></a>项目概览</h2><table>
<thead>
<tr>
<th>项</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong>引擎</strong></td>
<td>Unreal Engine 5.3</td>
</tr>
<tr>
<td><strong>类型</strong></td>
<td>第三人称射击（TPS）动画系统 Demo</td>
</tr>
<tr>
<td><strong>核心标签</strong></td>
<td>Animation Blueprint · Blend Space · Montage · IK Retargeting · Enhanced Input</td>
</tr>
</tbody></table>
<p>一个聚焦 UE5 动画系统的 TPS 角色 Demo。核心产物是一个完整的 <strong>Animation Blueprint</strong>（ABP_MyCharacter），包含完整的移动状态机、7 个 Blend Space、4 个 Animation Montage，配合 IK Retargeter 将 Mannequin 动画重定向到自定义角色模型。</p>
<hr>
<h2 id="一、Animation-Blueprint-总体架构"><a href="#一、Animation-Blueprint-总体架构" class="headerlink" title="一、Animation Blueprint 总体架构"></a>一、Animation Blueprint 总体架构</h2><p>ABP 是 UE 动画系统的枢纽。本项目的 ABP_MyCharacter 设计分三层：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ABP_MyCharacter</span><br><span class="line">├── Event Graph          ← 每帧从 C++ / BP 拿数据，更新动画变量</span><br><span class="line">├── Anim Graph (主输出)</span><br><span class="line">│   ├── Locomotion State Machine  ← 移动状态机</span><br><span class="line">│   │   ├── Idle ──→ Walk ──→ Run</span><br><span class="line">│   │   ├── Idle ──→ Crouch</span><br><span class="line">│   │   ├── Idle ──→ Jump ──→ Fall ──→ Land</span><br><span class="line">│   │   └── TurnInPlace</span><br><span class="line">│   └── Layered Blend Per Bone    ← 上半身 Montage 叠加</span><br><span class="line">│       ├── Base (Locomotion)</span><br><span class="line">│       └── Upper Body Slot (Fire/Reload/Punch)</span><br><span class="line">└── Post Process Anim BP          ← Control Rig Foot IK</span><br></pre></td></tr></table></figure>

<h3 id="数据来源：C-→-Anim-BP"><a href="#数据来源：C-→-Anim-BP" class="headerlink" title="数据来源：C++ → Anim BP"></a>数据来源：C++ → Anim BP</h3><p>动画蓝图本身不持有数据，依赖 C++ Character 层每帧计算后传入：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// MyCharacter 中，Enhanced Input 每帧驱动 CharacterMovement</span></span><br><span class="line"><span class="comment">// AnimBP 通过 GetVelocity() / GetActorRotation() 等原生接口取值</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// BaseGun 中暴露武器动画类型</span></span><br><span class="line"><span class="function">TEnumAsByte&lt;EWeaponAnimType&gt; <span class="title">GetWeaponType</span><span class="params">()</span></span>;  <span class="comment">// PistolAnim / RifleAnim</span></span><br></pre></td></tr></table></figure>

<p>Anim BP 的 Event Graph 中做方向计算：</p>
<ul>
<li><strong>Speed</strong> &#x3D; <code>Velocity.Size()</code> — 区分 Idle &#x2F; Walk &#x2F; Run</li>
<li><strong>Direction</strong> &#x3D; <code>CalculateDirection(Velocity, ActorRotation)</code> — Blend Space 的横轴</li>
<li><strong>bIsCrouching</strong> &#x3D; <code>CharacterMovement.IsCrouching()</code></li>
<li><strong>bIsInAir</strong> &#x3D; <code>CharacterMovement.IsFalling()</code></li>
<li><strong>WeaponType</strong> &#x3D; 从 <code>BaseGun::GetWeaponType()</code> 取值，决定武器动画分支</li>
</ul>
<hr>
<h2 id="二、八向移动的实现步骤"><a href="#二、八向移动的实现步骤" class="headerlink" title="二、八向移动的实现步骤"></a>二、八向移动的实现步骤</h2><p>八向移动的核心思路：<strong>用 Blend Space 把方向离散化，再用 Animation BP 的 State Machine 按速度分状态。</strong></p>
<h3 id="Step-1：准备动画资产"><a href="#Step-1：准备动画资产" class="headerlink" title="Step 1：准备动画资产"></a>Step 1：准备动画资产</h3><p>为每个移动状态准备 8 个方向的动画序列：</p>
<table>
<thead>
<tr>
<th>方向</th>
<th>角度</th>
<th>Walk 示例</th>
<th>Run 示例</th>
</tr>
</thead>
<tbody><tr>
<td>Forward</td>
<td>0°</td>
<td><code>anim_walk_forward</code></td>
<td><code>anim_run_forward</code></td>
</tr>
<tr>
<td>Forward-Right</td>
<td>45°</td>
<td><code>anim_walk_rightforward</code></td>
<td><code>anim_run_rightforward</code></td>
</tr>
<tr>
<td>Right</td>
<td>90°</td>
<td><code>anim_walk_right</code></td>
<td><code>anim_run_right</code></td>
</tr>
<tr>
<td>Backward-Right</td>
<td>135°</td>
<td><code>anim_walk_rightbackward</code></td>
<td><code>anim_run_rightbackward</code></td>
</tr>
<tr>
<td>Backward</td>
<td>180°&#x2F;-180°</td>
<td><code>anim_walk_backward</code></td>
<td><code>anim_run_backward</code></td>
</tr>
<tr>
<td>Backward-Left</td>
<td>-135°</td>
<td><code>anim_walk_leftbackward</code></td>
<td><code>anim_run_leftbackward</code></td>
</tr>
<tr>
<td>Left</td>
<td>-90°</td>
<td><code>anim_walk_left</code></td>
<td><code>anim_run_left</code></td>
</tr>
<tr>
<td>Forward-Left</td>
<td>-45°</td>
<td><code>anim_walk_leftforward</code></td>
<td><code>anim_run_leftforward</code></td>
</tr>
</tbody></table>
<p>本项目为 8 方向的每个组合都做了独立动画（Walk × 8、Run × 8、Crouch × 8、WalkAim × 8、EquipWalk × 8 等），这是实现高质量八向移动的基础。</p>
<h3 id="Step-2：创建-Blend-Space"><a href="#Step-2：创建-Blend-Space" class="headerlink" title="Step 2：创建 Blend Space"></a>Step 2：创建 Blend Space</h3><p>为每个移动模式创建 Blend Space（水平轴 &#x3D; Direction，范围 -180~180）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">BS_Walk:  [ -180° | -135° | -90° | -45° | 0° | 45° | 90° | 135° | 180° ]</span><br><span class="line">BS_Run:   [ -180° | -135° | -90° | -45° | 0° | 45° | 90° | 135° | 180° ]</span><br><span class="line">BS_Crouch:[ -180° | -135° | -90° | -45° | 0° | 45° | 90° | 135° | 180° ]</span><br></pre></td></tr></table></figure>

<p><strong>关键设置：</strong></p>
<ul>
<li>Interpolation Time：尽量小（0.1~0.2s），否则方向切换有”飘”感</li>
<li>每个动画的权重区域不要过大，否则相邻动画会被错误激活</li>
<li>建议开启 <code>Display Editor Vertices</code> → <code>Grid</code> 模式来可视化每个动画的权重区域</li>
</ul>
<h3 id="Step-3：过渡-Blend-Space"><a href="#Step-3：过渡-Blend-Space" class="headerlink" title="Step 3：过渡 Blend Space"></a>Step 3：过渡 Blend Space</h3><p>Idle 和 Move 之间不能直接切换——需要过渡动画：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">BS_IdleToWalk:   [ Idle → Walk (8 dir)  ]</span><br><span class="line">BS_IdleToRun:    [ Idle → Run (8 dir)   ]</span><br><span class="line">BS_IdleToCrouch: [ Idle → Crouch (8 dir)]</span><br></pre></td></tr></table></figure>

<p>这些过渡 Blend Space 在 State Machine 中作为 <strong>Transition Rule 的 Blend 资产</strong> 使用——当状态切换时，过渡阶段播放它们而不是立刻切到目标 Blend Space。</p>
<h3 id="Step-4：构建-State-Machine"><a href="#Step-4：构建-State-Machine" class="headerlink" title="Step 4：构建 State Machine"></a>Step 4：构建 State Machine</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">        ┌─────────────────────────────┐</span><br><span class="line">        │         Idle State          │</span><br><span class="line">        │   (BS_Idle, BS_Turn)        │</span><br><span class="line">        └──────┬──────────┬───────────┘</span><br><span class="line">    Speed&gt;150  │          │  Speed&gt;10,&lt;150</span><br><span class="line">               ▼          ▼</span><br><span class="line">┌──────────┐       ┌──────────────┐</span><br><span class="line">│ Run State│       │  Walk State   │</span><br><span class="line">│ (BS_Run) │       │ (BS_Walk)     │</span><br><span class="line">└────┬─────┘       └──────┬───────┘</span><br><span class="line">     │     Speed&lt;150     │</span><br><span class="line">     └───────────────────┘ Speed&lt;10</span><br><span class="line">               │</span><br><span class="line"> Speed&lt;10      ▼</span><br><span class="line">┌──────────────────────┐</span><br><span class="line">│  IdleToRun/WalkStop  │  ← Transition Blend Space</span><br><span class="line">└──────────────────────┘</span><br></pre></td></tr></table></figure>

<p><strong>状态机设计要点：</strong></p>
<ol>
<li><strong>Idle 作为 Hub</strong>：所有移动状态最终都回到 Idle，避免状态爆炸</li>
<li><strong>Speed 阈值驱动流转</strong>：<code>Speed &lt; 10</code> → Idle，<code>10 &lt; Speed &lt; 150</code> → Walk，<code>Speed &gt; 150</code> → Run</li>
<li><strong>过渡使用 Blend Space 而非硬切</strong>：Transition 的 <code>Blend Logic</code> 设为 <code>BlendSpace</code> 类型，引用对应的 IdleToMove Blend Space</li>
<li><strong>Crouch 独立子状态机</strong>：<code>bIsCrouching</code> 触发进入 Crouch 子树，避免和站立状态交叉污染</li>
</ol>
<h3 id="Step-5：叠加武器姿态"><a href="#Step-5：叠加武器姿态" class="headerlink" title="Step 5：叠加武器姿态"></a>Step 5：叠加武器姿态</h3><p>移动状态只管下半身。上半身的武器姿态通过 <strong>Layered Blend Per Bone</strong> 叠加：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Final Animation Pose</span><br><span class="line">├── Base Pose: Locomotion State Machine (全身)</span><br><span class="line">└── Blend Weight = 1.0, Bone Filter: Spine_01 ~ Head</span><br><span class="line">    └── Blend Poses by Enum (WeaponType)</span><br><span class="line">        ├── PistolAnim → AO_Aim_Pistol + anim_idle_aim</span><br><span class="line">        └── RifleAnim  → AO_Aim_Rifle + anim_idle_aimAR</span><br></pre></td></tr></table></figure>

<p><strong>关键节点：</strong></p>
<ul>
<li><code>Layered Blend Per Bone (Branch Filter = Spine_01)</code> — 上半身叠加，不影响移动</li>
<li><code>Blend Poses by Enum</code> — 按 <code>EWeaponAnimType</code> 枚举切换手枪&#x2F;步枪动画集</li>
<li>每个武器分支内部再用 <code>BlendSpace (AimOffset)</code> 处理 9 方向瞄准</li>
</ul>
<h3 id="八向移动常见坑"><a href="#八向移动常见坑" class="headerlink" title="八向移动常见坑"></a>八向移动常见坑</h3><table>
<thead>
<tr>
<th>问题</th>
<th>原因</th>
<th>解决</th>
</tr>
</thead>
<tbody><tr>
<td>方向切换有”吸附感”</td>
<td>Blend Space Interpolation 过大</td>
<td>设为 0.1~0.15</td>
</tr>
<tr>
<td>后退时动画”反转”</td>
<td>Root Motion 与方向不匹配</td>
<td>检查动画的 Root Motion Rotation</td>
</tr>
<tr>
<td>武器动画覆盖全身</td>
<td>Layered Blend Bone Filter 设错</td>
<td>确认只 Mask 上半身骨骼链</td>
</tr>
<tr>
<td>停步瞬间脚滑步</td>
<td>缺少 Stop 过渡动画</td>
<td>添加 WalkStop&#x2F;RunStop 带左右脚变体</td>
</tr>
</tbody></table>
<hr>
<h2 id="三、Animation-Montage-的搭建步骤"><a href="#三、Animation-Montage-的搭建步骤" class="headerlink" title="三、Animation Montage 的搭建步骤"></a>三、Animation Montage 的搭建步骤</h2><p>Montage 用于”一次性动作”——开火、换弹、闪避、近战。它是<strong>插播式</strong>的，播完自动退出，不会影响 Locomotion 状态机。</p>
<h3 id="Step-1：创建-Montage-资产"><a href="#Step-1：创建-Montage-资产" class="headerlink" title="Step 1：创建 Montage 资产"></a>Step 1：创建 Montage 资产</h3><p>每个动作一个 Montage，设置 Slot：</p>
<table>
<thead>
<tr>
<th>Montage</th>
<th>Slot Name</th>
<th>用途</th>
<th>Blend In</th>
<th>Blend Out</th>
</tr>
</thead>
<tbody><tr>
<td><code>AM_Fire</code></td>
<td><code>UpperBody</code></td>
<td>射击（只影响上半身）</td>
<td>0.05s</td>
<td>0.1s</td>
</tr>
<tr>
<td><code>AM_Reload</code></td>
<td><code>UpperBody</code></td>
<td>换弹</td>
<td>0.15s</td>
<td>0.2s</td>
</tr>
<tr>
<td><code>AM_Dodge</code></td>
<td><code>FullBody</code></td>
<td>翻滚闪避</td>
<td>0.1s</td>
<td>0.15s</td>
</tr>
<tr>
<td><code>AM_Punch</code></td>
<td><code>FullBody</code></td>
<td>近战攻击</td>
<td>0.1s</td>
<td>0.15s</td>
</tr>
</tbody></table>
<p><strong>Slot 设计方案：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">DefaultGroup.UpperBody   ← 上半身动作（Fire / Reload），不打断移动</span><br><span class="line">DefaultGroup.FullBody    ← 全身动作（Dodge / Punch），覆盖移动</span><br></pre></td></tr></table></figure>

<h3 id="Step-2：Anim-Graph-中配置-Slot-节点"><a href="#Step-2：Anim-Graph-中配置-Slot-节点" class="headerlink" title="Step 2：Anim Graph 中配置 Slot 节点"></a>Step 2：Anim Graph 中配置 Slot 节点</h3><p>在 Anim Graph 的输出 Pose 链路中插入 Slot 节点：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Locomotion State Machine (Output Pose)</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">Slot &quot;UpperBody&quot; ──→ LayeredBlendPerBone (BoneFilter: Spine_01 ~ Head)</span><br><span class="line">    │                        │</span><br><span class="line">    │  Base Pose (full body) │  Blend Weight = Montage 激活时 → 1.0</span><br><span class="line">    │                        │</span><br><span class="line">    ▼                        ▼</span><br><span class="line">Slot &quot;FullBody&quot; ──→ BlendWeights = 1.0 (覆盖全身)</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">Output Pose</span><br></pre></td></tr></table></figure>

<p><strong>重点：</strong> <code>UpperBody</code> Slot 在 Layered Blend Per Bone 之后、输出之前接入——这样 Montage 播上半身动画时，下半身继续播 Locomotion Pose。而 <code>FullBody</code> Slot 直接覆盖整个输出链。</p>
<h3 id="Step-3：在-Montage-中组织-Section"><a href="#Step-3：在-Montage-中组织-Section" class="headerlink" title="Step 3：在 Montage 中组织 Section"></a>Step 3：在 Montage 中组织 Section</h3><p>以 <code>AM_Fire</code> 为例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Section: &quot;FireStart&quot;  →  Section: &quot;FireLoop&quot;  →  Section: &quot;FireEnd&quot;</span><br><span class="line">    (举枪 0.1s)            (射击循环)              (收枪)</span><br></pre></td></tr></table></figure>

<ul>
<li>连射武器：Loop Section，按下鼠标时循环”FireLoop”，松开时跳”FireEnd”</li>
<li>单发武器：只播放 “FireStart” → “FireEnd”，不循环</li>
</ul>
<h3 id="Step-4：C-侧触发-Montage"><a href="#Step-4：C-侧触发-Montage" class="headerlink" title="Step 4：C++ 侧触发 Montage"></a>Step 4：C++ 侧触发 Montage</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// MyCharacter::Fire() — 简化版</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">AMyCharacter::Fire</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (CurrentGun &amp;&amp; !CurrentGun-&gt;<span class="built_in">NeedReload</span>() &amp;&amp; !CurrentGun-&gt;bIsReloading)</span><br><span class="line">    &#123;</span><br><span class="line">        CurrentGun-&gt;<span class="built_in">GunFire</span>();  <span class="comment">// 这会触发 PlayFireAnimation()</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// BaseGun::PlayFireAnimation() — 枪械自身动画</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ABaseGun::PlayFireAnimation</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    USkeletalMeshComponent* SKM = <span class="built_in">Cast</span>&lt;USkeletalMeshComponent&gt;(</span><br><span class="line">        <span class="built_in">GetComponentByClass</span>(USkeletalMeshComponent::<span class="built_in">StaticClass</span>()));</span><br><span class="line">    <span class="keyword">if</span> (SKM &amp;&amp; FireAnimationAsset)</span><br><span class="line">    &#123;</span><br><span class="line">        SKM-&gt;<span class="built_in">PlayAnimation</span>(FireAnimationAsset, <span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里有两层动画：</p>
<ol>
<li><strong>角色 Montage</strong>（<code>AM_Fire</code>）— 在 Character 的 Animation BP 中播放，控制角色身体姿态</li>
<li><strong>枪械动画</strong>（<code>FireAnimationAsset</code>）— 在 Gun 的 SkeletalMeshComponent 上播放，控制枪械自身的套筒&#x2F;弹匣运动</li>
</ol>
<p>两者通过 Anim BP 中的 Notify 或者 BP 端逻辑做同步。</p>
<h3 id="Montage-常见坑"><a href="#Montage-常见坑" class="headerlink" title="Montage 常见坑"></a>Montage 常见坑</h3><table>
<thead>
<tr>
<th>问题</th>
<th>原因</th>
<th>解决</th>
</tr>
</thead>
<tbody><tr>
<td>Montage 播了但看不见</td>
<td>Slot 节点没在 Anim Graph 中配置</td>
<td>检查 Slot 节点连接顺序</td>
</tr>
<tr>
<td>动作”卡”在最后一帧</td>
<td>Montage 没有正确结束</td>
<td>设置 <code>Blend Out Time</code> &gt; 0</td>
</tr>
<tr>
<td>上半身动作影响了腿</td>
<td>Slot 在 FullBody 层而非 UpperBody</td>
<td>确认 Slot 名称匹配</td>
</tr>
<tr>
<td>连发时 Montage 重新开始</td>
<td>重复 <code>PlayMontage</code> 未检查已播放</td>
<td>用 <code>Montage_IsPlaying()</code> 判断</td>
</tr>
</tbody></table>
<hr>
<h2 id="四、整体数据流回顾"><a href="#四、整体数据流回顾" class="headerlink" title="四、整体数据流回顾"></a>四、整体数据流回顾</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Enhanced Input          C++ Character            Animation BP</span><br><span class="line">─────────────          ──────────────           ─────────────</span><br><span class="line">IA_MoveForward  ──→  AddMovementInput  ──→  Velocity</span><br><span class="line">IA_MoveRight                                          │</span><br><span class="line">IA_Run          ──→  MaxWalkSpeed=600          Speed ──→ State Machine</span><br><span class="line">IA_Crouch       ──→  Crouch()             bIsCrouching    │</span><br><span class="line">IA_Aim          ──→  (BP event)          bIsAiming        │</span><br><span class="line">IA_Fire         ──→  AMyCharacter::Fire()                  │</span><br><span class="line">                         │                                 ▼</span><br><span class="line">                    ABaseGun::GunFire()         Blend Space (Direction)</span><br><span class="line">                         │                           │</span><br><span class="line">                    PlayFireAnimation()        Locomotion Pose</span><br><span class="line">                    ICRRecoilInterface               │</span><br><span class="line">                         │                           ▼</span><br><span class="line">                    RecoilComponent          Slot &quot;UpperBody&quot;</span><br><span class="line">                    SpreadRecoilComponent    └── AM_Fire (Montage)</span><br><span class="line">                                                    │</span><br><span class="line">                                                    ▼</span><br><span class="line">                                            Layered Blend Per Bone</span><br><span class="line">                                                    │</span><br><span class="line">                                                    ▼</span><br><span class="line">                                              Output Pose</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="五、可改进点"><a href="#五、可改进点" class="headerlink" title="五、可改进点"></a>五、可改进点</h2><table>
<thead>
<tr>
<th>问题</th>
<th>改进方向</th>
</tr>
</thead>
<tbody><tr>
<td>所有移动动画都是手动导入的序列帧</td>
<td>用 <strong>Motion Matching</strong>（UE 5.4+）替代手配 Blend Space，减少动画数量 + 提升表现</td>
</tr>
<tr>
<td>Montage 和 C++ 之间靠 <code>bIsReloading</code> 等 bool 变量同步</td>
<td>改用 <strong>Gameplay Tags</strong> 做动作状态标记，更解耦</td>
</tr>
<tr>
<td>枪械动画和角色 Montage 分开播放，同步靠 BP 端 Tick</td>
<td>用 <strong>Sync Group</strong> 或 <strong>Notify State</strong> 做精确帧同步</td>
</tr>
<tr>
<td>Control Rig Foot IK 只用在后处理</td>
<td>可以进一步用 Control Rig 做瞄准时的脊柱补偿（Spine Look At）</td>
</tr>
</tbody></table>
<hr>
<blockquote>
<p>如果你也在用 UE5 搭建角色动画系统，欢迎交流。</p>
</blockquote>
]]></content>
      <categories>
        <category>Portfolio</category>
      </categories>
      <tags>
        <tag>UE5</tag>
        <tag>Unreal</tag>
        <tag>Animation</tag>
        <tag>AnimationBlueprint</tag>
        <tag>BlendSpace</tag>
        <tag>Montage</tag>
      </tags>
  </entry>
  <entry>
    <title>Crunch：基于 UE5 GAS 的第三人称动作 RPG Demo —— GAS 架构与网络同步</title>
    <url>/2026/08/05/crunch-ue-gamplay-ability-system-and-network-sync/</url>
    <content><![CDATA[<h2 id="项目概览"><a href="#项目概览" class="headerlink" title="项目概览"></a>项目概览</h2><table>
<thead>
<tr>
<th>项</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong>引擎</strong></td>
<td>Unreal Engine 5.4</td>
</tr>
<tr>
<td><strong>类型</strong></td>
<td>第三人称动作 RPG &#x2F; MOBA 风格</td>
</tr>
<tr>
<td><strong>角色资源</strong></td>
<td>Epic Paragon（Crunch + Minions）</td>
</tr>
<tr>
<td><strong>源码规模</strong></td>
<td>~80 个 C++ 文件，覆盖 GAS &#x2F; AI &#x2F; UI &#x2F; Inventory</td>
</tr>
<tr>
<td><strong>核心标签</strong></td>
<td><code>GAS</code> <code>Network Replication</code> <code>Combo System</code> <code>RPG Attributes</code> <code>Enhanced Input</code></td>
</tr>
</tbody></table>
<p>一个从零搭建的 Unreal 动作游戏 Demo。角色是 Paragon 的 Crunch——一个近战拳击手，核心战斗围绕<strong>轻击连招、浮空 UpperCut、地面 AOE 轰炸</strong>展开。底层完全基于 <strong>Gameplay Ability System</strong> 搭建，”服务器权威”模型做网络同步。</p>
<iframe src="//player.bilibili.com/player.html?bvid=BV1DfMC6cELg" scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true" width="100%" height="480"></iframe>

<blockquote>
<p>📦 <strong>高清下载</strong>: <a href="https://easylink.cc/f409p2">EasyLink云链</a></p>
</blockquote>
<hr>
<h2 id="一、为什么选-GAS？架构总览"><a href="#一、为什么选-GAS？架构总览" class="headerlink" title="一、为什么选 GAS？架构总览"></a>一、为什么选 GAS？架构总览</h2><h3 id="1-1-决策背景"><a href="#1-1-决策背景" class="headerlink" title="1.1 决策背景"></a>1.1 决策背景</h3><p>做动作游戏 Demo 时，技能系统有几种常见路线：</p>
<table>
<thead>
<tr>
<th>方案</th>
<th>优点</th>
<th>痛点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>手写状态机</strong></td>
<td>完全可控，无黑盒</td>
<td>技能一多，状态爆炸；Buff&#x2F;Debuff 要自己写一套</td>
</tr>
<tr>
<td><strong>插件&#x2F;中间件</strong></td>
<td>开箱即用</td>
<td>文档质量参差不齐，升级引擎时可能被 break</td>
</tr>
<tr>
<td><strong>Epic GAS</strong></td>
<td>为多人游戏设计，内置网络同步，与引擎深度集成</td>
<td>学习曲线陡峭，纯 C++ 开发，没有蓝图节点面板</td>
</tr>
</tbody></table>
<p>选 GAS 的核心原因：<strong>网络同步是”内置”的，不是”后加”的</strong>。</p>
<p>做技能系统时，如果你的 Actor 需要 <code>HasAuthority()</code> → 做伤害 → 再手动 <code>Client_RPC</code> 去播特效，这个模式写三五个技能还行，写到第十个的时候一定会出问题——要么忘了权限判断导致作弊，要么忘了客户端回调导致特效不播。</p>
<p>GAS 把”技能激活 → 伤害计算 → 效果应用 → 状态同步”这条链路标准化了。网络同步发生在 <strong>GameplayEffect</strong> 和 <strong>GameplayTag</strong> 这两个层级，而这些是 GAS 自动帮你处理的。你不用在每个技能里写 RPC。</p>
<h3 id="1-2-整体架构"><a href="#1-2-整体架构" class="headerlink" title="1.2 整体架构"></a>1.2 整体架构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────────┐</span><br><span class="line">│                     ACPlayerCharacter                     │</span><br><span class="line">│  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐  │</span><br><span class="line">│  │ Enhanced    │  │ Camera +     │  │ Inventory      │  │</span><br><span class="line">│  │ Input       │  │ SpringArm    │  │ Component      │  │</span><br><span class="line">│  └──────┬──────┘  └──────────────┘  └────────────────┘  │</span><br><span class="line">│         │                                                │</span><br><span class="line">│  ┌──────▼──────────────────────────────────────────────┐ │</span><br><span class="line">│  │            UCAbilitySystemComponent (ASC)            │ │</span><br><span class="line">│  │  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │ │</span><br><span class="line">│  │  │ Abilities│  │ Basic    │  │ Passive          │  │ │</span><br><span class="line">│  │  │ (技能)   │  │ Abilities│  │ Abilities        │  │ │</span><br><span class="line">│  │  └──────────┘  └──────────┘  └──────────────────┘  │ │</span><br><span class="line">│  │  ┌──────────────────────────────────────────────┐   │ │</span><br><span class="line">│  │  │               AttributeSets                  │   │ │</span><br><span class="line">│  │  │  UCAttributeSet     │  UCHeroAttributeSet    │   │ │</span><br><span class="line">│  │  │  Health / Mana      │  Level / Exp / Intel   │   │ │</span><br><span class="line">│  │  │  AttackDamage / Def │  Strength / Gold       │   │ │</span><br><span class="line">│  │  └──────────────────────────────────────────────┘   │ │</span><br><span class="line">│  └─────────────────────────────────────────────────────┘ │</span><br><span class="line">└─────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>

<p><strong>双层 AttributeSet 设计</strong>是这个项目的一个关键架构决策：</p>
<ul>
<li><strong><code>UCAttributeSet</code></strong> — 战斗属性：Health、Mana、AttackDamage、Armor、MoveSpeed。所有角色共用，包括 AI 小兵。</li>
<li><strong><code>UCHeroAttributeSet</code></strong> — 英雄属性：Level、Experience、Intelligence、Strength、Gold、UpgradePoint。只挂在玩家角色上。</li>
</ul>
<p>这样做的原因是：AI 小兵不需要等级和金币系统。如果全部塞进一个 AttributeSet，每个 Minion 身上都会挂着无意义的 <code>Gold</code> 和 <code>Experience</code> 属性——浪费网络带宽，也污染语义。</p>
<hr>
<h2 id="二、GAS-核心实现"><a href="#二、GAS-核心实现" class="headerlink" title="二、GAS 核心实现"></a>二、GAS 核心实现</h2><h3 id="2-1-自定义-ASC：ServerSideInit-ClientSideInit"><a href="#2-1-自定义-ASC：ServerSideInit-ClientSideInit" class="headerlink" title="2.1 自定义 ASC：ServerSideInit &#x2F; ClientSideInit"></a>2.1 自定义 ASC：ServerSideInit &#x2F; ClientSideInit</h3><p>这是整个项目最关键的网络架构模式。看一下 <code>CCharacter</code> 的初始化流程：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ── 服务器端：PossessedBy 时调用（AI 角色在服务器被接管）</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ACCharacter::PossessedBy</span><span class="params">(AController* NewController)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Super::<span class="built_in">PossessedBy</span>(NewController);</span><br><span class="line">    <span class="keyword">if</span> (NewController &amp;&amp; !NewController-&gt;<span class="built_in">IsPlayerController</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">ServerSideInit</span>();  <span class="comment">// AI 角色走这条路</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ── 客户端：PostNetInit 时调用（复制体到达客户端后）</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ACCharacter::PostNetInit</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Super::<span class="built_in">PostNetInit</span>();</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">GetLocalRole</span>() != ROLE_Authority)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">ClientSideInit</span>();  <span class="comment">// 客户端复制体走这条路</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>ServerSideInit()</code> 做了三件事：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">UCAbilitySystemComponent::ServerSideInit</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">InitializeBaseAttributes</span>();  <span class="comment">// ① 从 DataTable 读取基础属性</span></span><br><span class="line">    <span class="built_in">ApplyInitialEffects</span>();       <span class="comment">// ② 应用初始 GameplayEffect（满血满蓝）</span></span><br><span class="line">    <span class="built_in">GiveInitialAbilities</span>();      <span class="comment">// ③ 授予技能</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>ClientSideInit()</code> 只做一件事：调用 <code>InitAbilityActorInfo(this, this)</code>，把 ASC 的 ActorInfo 绑定好，这样客户端才能收到属性复制和 GameplayTag 更新。</p>
<blockquote>
<p><strong>关键设计原则：服务器初始化”数据”，客户端只初始化”连接”</strong>。服务器拥有权威数据（属性、技能列表、效果），客户端通过 GAS 内置的属性复制机制自动同步，不需要手动 RPC 把属性一个个发过去。</p>
</blockquote>
<h3 id="2-2-自定义-GameplayAbility-基类"><a href="#2-2-自定义-GameplayAbility-基类" class="headerlink" title="2.2 自定义 GameplayAbility 基类"></a>2.2 自定义 GameplayAbility 基类</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">UCLASS</span>()</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UCGameplayAbility</span> : <span class="keyword">public</span> UGameplayAbility</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ── 通用工具方法，所有技能继承</span></span><br><span class="line">    <span class="function">TArray&lt;FHitResult&gt; <span class="title">GetHitResultsFromSweepLocationTargetData</span><span class="params">(...)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">PushTarget</span><span class="params">(AActor* InTarget, <span class="type">const</span> FVector&amp; InVelocity)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">PushTargets</span><span class="params">(<span class="type">const</span> TArray&lt;AActor*&gt;&amp; InTargets, <span class="type">const</span> FVector&amp; PushVelocity)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">ApplyGameplayEffectToHitResultActor</span><span class="params">(<span class="type">const</span> FHitResult&amp; Hit, ...)</span></span>;</span><br><span class="line">    <span class="function">ACharacter* <span class="title">GetOwningAvatarCharacter</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>每个技能都需要的操作——从 TargetData 取命中结果、对命中目标施加击退、对命中目标施加 GE——封装到基类里。实际技能只需要关注自己的逻辑。</p>
<h3 id="2-3-Combo-连招系统"><a href="#2-3-Combo-连招系统" class="headerlink" title="2.3 Combo 连招系统"></a>2.3 Combo 连招系统</h3><p>Combo 是这个项目最复杂的技能逻辑，核心思路是 <strong>GameplayEvent 链式驱动</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">玩家按下攻击键</span><br><span class="line">    → GA_Combo::ActivateAbility()</span><br><span class="line">    → 播放 ComboMontage（动画）</span><br><span class="line">    → AnimNotifier 在特定帧触发 ComboChangedEvent</span><br><span class="line">    → ComboChangedEventReceived() 切换 Combo 段位</span><br><span class="line">    → AnimNotifier 在伤害判定帧触发 ComboTargetEvent</span><br><span class="line">    → DoDamage() 对扇形范围内的敌人施加对应段位的 GE</span><br><span class="line">    → 等待下一次按键输入 → TryCommitCombo() 进入下一段</span><br></pre></td></tr></table></figure>

<p>关键代码——按键等待与 Combo 切换：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">UGA_Combo::SetupWaitComboInputPress</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 等待玩家在窗口期内再次按下攻击键</span></span><br><span class="line">    <span class="built_in">WaitInputPress</span>(HandleInputPressed, ...);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UGA_Combo::HandleInputPressed</span><span class="params">(<span class="type">float</span> TimeWaited)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">TryCommitCombo</span>();  <span class="comment">// 进入下一段 Combo</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UGA_Combo::ComboChangedEventReceived</span><span class="params">(FGameplayEventData Data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    NextComboName = Data.EventMagnitude &gt; <span class="number">0</span> </span><br><span class="line">        ? <span class="built_in">FName</span>(*FString::<span class="built_in">Printf</span>(<span class="built_in">TEXT</span>(<span class="string">&quot;Combo%d&quot;</span>), (<span class="type">int</span>)Data.EventMagnitude))</span><br><span class="line">        : NAME_None;</span><br><span class="line">    <span class="built_in">SetupWaitComboInputPress</span>();  <span class="comment">// 继续等待下一段输入</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>不同 Combo 段位施加不同的 GameplayEffect（伤害系数不同），通过 <code>DamageEffectMap</code> 配置：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">UPROPERTY</span>(EditDefaultsOnly)</span><br><span class="line">TMap&lt;FName, TSubclassOf&lt;UGameplayEffect&gt;&gt; DamageEffectMap;</span><br><span class="line"><span class="comment">// &quot;Combo1&quot; → GE_Combo1_Damage</span></span><br><span class="line"><span class="comment">// &quot;Combo2&quot; → GE_Combo2_Damage  (伤害递增)</span></span><br><span class="line"><span class="comment">// &quot;Combo3&quot; → GE_Combo3_Damage  (终结段最大伤害)</span></span><br></pre></td></tr></table></figure>

<h3 id="2-4-UpperCut-浮空-Combo-整合"><a href="#2-4-UpperCut-浮空-Combo-整合" class="headerlink" title="2.4 UpperCut 浮空 + Combo 整合"></a>2.4 UpperCut 浮空 + Combo 整合</h3><p>UpperCut 不仅是一个独立技能，它还能<strong>接入 Combo 系统</strong>——在 Combo 的任意段位之后接 UpperCut，伤害系数随 Combo 段位变化：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">UPROPERTY</span>(EditDefaultsOnly, Category=<span class="string">&quot;Combo&quot;</span>)</span><br><span class="line">TMap&lt;FName, FGenericDamageEffectDef&gt; ComboDamageMap;</span><br><span class="line"><span class="comment">// 从 Combo1/Combo2/Combo3 接 UpperCut → 不同的伤害值</span></span><br></pre></td></tr></table></figure>

<p>UpperCut 的浮空效果通过 <code>LaunchDamageEffect</code>（一个 GE）实现——给目标施加一个向上的速度：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">UPROPERTY</span>(EditDefaultsOnly, Category=<span class="string">&quot;Launch&quot;</span>)</span><br><span class="line"><span class="type">float</span> UpperCutLaunchSpeed = <span class="number">1000.f</span>;     <span class="comment">// 浮空初速度</span></span><br><span class="line"><span class="type">float</span> UpperCutComboHoldSpeed = <span class="number">0.f</span>;     <span class="comment">// 后续段减速→滞空</span></span><br></pre></td></tr></table></figure>

<h3 id="2-5-Ground-Blast-地面-AOE"><a href="#2-5-Ground-Blast-地面-AOE" class="headerlink" title="2.5 Ground Blast 地面 AOE"></a>2.5 Ground Blast 地面 AOE</h3><p>这个技能展示了 GAS 的 <strong>TargetActor</strong> 机制——先用 GroundPick TargetActor 在地面上选点，确认后再释放：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">UGA_GroundBlast::ActivateAbility</span><span class="params">(...)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// ① 生成 TargetActor，玩家通过光标/摇杆在地面选点</span></span><br><span class="line">    <span class="comment">// ② TargetActor 返回 TargetData（包含选中位置）</span></span><br><span class="line">    <span class="comment">// ③ TargetConfirmed() → 在目标位置播放 GameplayCue（特效）→ 施加伤害</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="三、网络同步模型"><a href="#三、网络同步模型" class="headerlink" title="三、网络同步模型"></a>三、网络同步模型</h2><h3 id="3-1-核心原则：Server-Authoritative"><a href="#3-1-核心原则：Server-Authoritative" class="headerlink" title="3.1 核心原则：Server Authoritative"></a>3.1 核心原则：Server Authoritative</h3><p>整个项目的网络模型遵循 UE 的标准 Server-Authoritative 模型：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────┐         ┌──────────────────────┐</span><br><span class="line">│        SERVER         │         │        CLIENT         │</span><br><span class="line">│  ┌────────────────┐   │  Replicate  │  ┌────────────────┐  │</span><br><span class="line">│  │ AttributeSet   │───┼───────────→│  │ AttributeSet   │  │</span><br><span class="line">│  │ (权威数据)     │   │  OnRep     │  │ (只读副本)     │  │</span><br><span class="line">│  └────────────────┘   │            │  └────────────────┘  │</span><br><span class="line">│  ┌────────────────┐   │            │  ┌────────────────┐  │</span><br><span class="line">│  │ GameplayTags   │───┼───────────→│  │ GameplayTags   │  │</span><br><span class="line">│  │ (权威状态)     │   │  Replicate │  │ (只读副本)     │  │</span><br><span class="line">│  └────────────────┘   │            │  └────────────────┘  │</span><br><span class="line">│         ▲              │            │         │             │</span><br><span class="line">│         │              │            │         │             │</span><br><span class="line">│  Server RPC ◄──────────┼────────────┼─────────┘             │</span><br><span class="line">│  (技能升级/购买)       │   Client→  │   (按键输入)          │</span><br><span class="line">│                         │   Server   │                       │</span><br><span class="line">└──────────────────────┘         └──────────────────────┘</span><br></pre></td></tr></table></figure>

<p><strong>伤害计算、死亡判定、等级提升全部发生在服务器</strong>。客户端只负责：</p>
<ol>
<li>发送输入（通过 Enhanced Input → Ability Input Tag）</li>
<li>接收属性同步（OnRep 回调驱动 UI）</li>
<li>接收 GameplayCue（特效&#x2F;音效在客户端本地播放）</li>
</ol>
<h3 id="3-2-属性同步：OnRep-驱动-UI"><a href="#3-2-属性同步：OnRep-驱动-UI" class="headerlink" title="3.2 属性同步：OnRep 驱动 UI"></a>3.2 属性同步：OnRep 驱动 UI</h3><p>每个复制的属性都声明了 <code>ReplicatedUsing</code>：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">UPROPERTY</span>(ReplicatedUsing = OnRep_Health)</span><br><span class="line">FGameplayAttributeData Health;</span><br><span class="line"></span><br><span class="line"><span class="built_in">UFUNCTION</span>()</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">OnRep_Health</span><span class="params">(<span class="type">const</span> FGameplayAttributeData&amp; OldValue)</span></span>;</span><br></pre></td></tr></table></figure>

<p>当客户端收到属性同步时，GAS 自动触发 <code>OnRep_*</code>，然后广播 <code>AttributeValueChangeDelegate</code>。UI Widget 通过绑定这个 Delegate 自动更新——不需要手动调用任何刷新函数。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// UI 绑定：一行代码完成</span></span><br><span class="line">AbilitySystemComponent-&gt;<span class="built_in">GetGameplayAttributeValueChangeDelegate</span>(</span><br><span class="line">    UCAttributeSet::<span class="built_in">GetHealthAttribute</span>()</span><br><span class="line">).<span class="built_in">AddUObject</span>(<span class="keyword">this</span>, &amp;UStatsGauge::OnHealthChanged);</span><br></pre></td></tr></table></figure>

<h3 id="3-3-关键权限判断模式"><a href="#3-3-关键权限判断模式" class="headerlink" title="3.3 关键权限判断模式"></a>3.3 关键权限判断模式</h3><p>代码中有大量 <code>HasAuthority()</code> 判断，确保逻辑只在正确的端执行：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ✅ 经验升级——只在服务器计算</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UCAbilitySystemComponent::OnExperienceUpdated</span><span class="params">(<span class="type">const</span> FOnAttributeChangeData&amp; ChangeData)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">GetOwner</span>() || !<span class="built_in">GetOwner</span>()-&gt;<span class="built_in">HasAuthority</span>())</span><br><span class="line">        <span class="keyword">return</span>;  <span class="comment">// 客户端直接返回，不做升级计算</span></span><br><span class="line">    <span class="comment">// ...曲线查表、升级逻辑...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ✅ 死亡触发——只在服务器判定</span></span><br><span class="line"><span class="keyword">if</span> (ChangeData.NewValue &lt;= <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 施加 Death GE → 发送 Dead Event → 触发死亡序列</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ✅ 技能升级——客户端 RPC → 服务器执行 → 客户端回调</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">UCAbilitySystemComponent::Server_UpgradeAbilityWithID_Implementation</span><span class="params">(ECAbilityInputID InputID)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 服务器：检查升级点数 → 提升技能等级 → MarkAbilitySpecDirty</span></span><br><span class="line">    <span class="built_in">Client_AbilitySpecLevelUpdated</span>(AbilitySpec-&gt;Handle, AbilitySpec-&gt;Level);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-4-完全不用手动-RPC-的地方"><a href="#3-4-完全不用手动-RPC-的地方" class="headerlink" title="3.4 完全不用手动 RPC 的地方"></a>3.4 完全不用手动 RPC 的地方</h3><p>以下流程<strong>完全由 GAS 自动处理</strong>，不需要写一行 RPC：</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>GAS 机制</th>
</tr>
</thead>
<tbody><tr>
<td>技能激活&#x2F;取消</td>
<td>Ability Spec 的 <code>IsActive</code> 通过 ASC 同步</td>
</tr>
<tr>
<td>属性变化</td>
<td>AttributeSet 的 <code>ReplicatedUsing</code> → OnRep → Delegate</td>
</tr>
<tr>
<td>状态变化（死亡&#x2F;眩晕）</td>
<td>GameplayTag 复制 → <code>RegisterGameplayTagEvent</code> 回调</td>
</tr>
<tr>
<td>技能冷却</td>
<td><code>FGameplayAbilitySpec</code> 内置 Cooldown 计时，自动同步</td>
</tr>
<tr>
<td>伤害数字&#x2F;特效</td>
<td>GameplayCue 在客户端本地触发（非 RPC）</td>
</tr>
</tbody></table>
<h3 id="3-5-一个反例：GameplayCue-的-RPC-优化"><a href="#3-5-一个反例：GameplayCue-的-RPC-优化" class="headerlink" title="3.5 一个反例：GameplayCue 的 RPC 优化"></a>3.5 一个反例：GameplayCue 的 RPC 优化</h3><p>Git 记录里有一条很说明问题的 commit：</p>
<blockquote>
<p><code>Optimize the Overflow RPC cues, move it locally, and fix the camera shaking bug</code></p>
</blockquote>
<p>GameplayCue 在 UE5 中默认是 RPC 方式发送的（<code>AddGameplayCueToOwner</code>），在连招频繁触发伤害判定时，多个 Cue 的 RPC 会堆积。这条 commit 把 Cue 改为本地触发——既然 Cue 不涉及 Gameplay 逻辑，完全可以在客户端本地播放，不需要绕服务器一圈。</p>
<blockquote>
<p><strong>教训：GAS 的默认设置不一定适合你的场景</strong>。GameplayCue 默认走 RPC 是为了保证服务器也能”看见” Cue 做后续逻辑，但如果你的 Cue 纯视觉，local 就足够了。</p>
</blockquote>
<hr>
<h2 id="四、关键踩坑记录"><a href="#四、关键踩坑记录" class="headerlink" title="四、关键踩坑记录"></a>四、关键踩坑记录</h2><h3 id="5-1-GAS-的”纯-C-”代价"><a href="#5-1-GAS-的”纯-C-”代价" class="headerlink" title="5.1 GAS 的”纯 C++”代价"></a>5.1 GAS 的”纯 C++”代价</h3><p>GAS 不像很多 UE 系统那样有蓝图面板。技能激活逻辑、GE 效果计算、属性变化回调——全部在 C++ 里。好处是代码可读、可调试、可 diff；坏处是每改一个伤害公式都要重新编译。</p>
<blockquote>
<p>平衡策略：<strong>逻辑写 C++，配置写蓝图&#x2F;DataAsset</strong>。技能的 <code>EditDefaultsOnly</code> 属性（伤害系数、动画蒙太奇、GE 映射表）都暴露给蓝图，策划配置不需要动 C++。</p>
</blockquote>
<h3 id="5-2-属性同步的”抖动”问题"><a href="#5-2-属性同步的”抖动”问题" class="headerlink" title="5.2 属性同步的”抖动”问题"></a>5.2 属性同步的”抖动”问题</h3><p><code>PreAttributeChange</code> 是实现属性 Clamp 的地方（Health &#x3D; Clamp(NewValue, 0, MaxHealth)），但文档写得很清楚：<strong>不要在这里触发 Gameplay 逻辑</strong>。因为 <code>PreAttributeChange</code> 可能在属性复制的中间状态被调用，此时 MaxHealth 可能还没同步过来。</p>
<p>正确做法：在 <code>PostGameplayEffectExecute</code> 里做逻辑响应（死亡判定、UI 更新）。</p>
<h3 id="5-3-死亡状态机与-GAS-的配合"><a href="#5-3-死亡状态机与-GAS-的配合" class="headerlink" title="5.3 死亡状态机与 GAS 的配合"></a>5.3 死亡状态机与 GAS 的配合</h3><p>死亡不是”Health ≤ 0 → 播死亡动画”这么简单。这里涉及：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Health ≤ 0</span><br><span class="line">  → AddLooseGameplayTag(Dead)       // 标记死亡</span><br><span class="line">  → Apply GE_Death (Infinite)        // 施加无限时长死亡 GE（阻止所有技能）</span><br><span class="line">  → SendGameplayEvent(Dead)          // 广播死亡事件</span><br><span class="line">  → DeathTagUpdated()                // GameplayTag 回调</span><br><span class="line">      → CancelAllAbilities()         // 取消所有进行中的技能</span><br><span class="line">      → PlayDeathAnimation()         // 播放死亡蒙太奇</span><br><span class="line">      → SetRagdollEnabled(true)      // 开始 Ragdoll 物理</span><br></pre></td></tr></table></figure>

<p>复活时倒序执行，并且<strong>只在服务器</strong>移除死亡 GE、重置位置到 StartSpot。</p>
<hr>
<h2 id="五、架构可改进点"><a href="#五、架构可改进点" class="headerlink" title="五、架构可改进点"></a>五、架构可改进点</h2><p>写给自己看的反思，也是给面试官展示”我知道哪里还不够好”：</p>
<table>
<thead>
<tr>
<th>问题</th>
<th>改进方案</th>
</tr>
</thead>
<tbody><tr>
<td><strong>技能配置分散</strong> — DataTable + DataAsset + 蓝图各自管一部分</td>
<td>统一用 DataAsset 做技能定义，一个 DA 描述一个技能的全部参数</td>
</tr>
<tr>
<td><strong>缺少 GameplayAbility 的蓝图层</strong> — 所有 GA 都是 C++ 子类</td>
<td>用 <code>UGameplayAbility</code> 基类 + <code>AbilityTask</code> 可以在蓝图中组装大部分技能</td>
</tr>
<tr>
<td><strong>没有 GAS 的单元测试</strong></td>
<td>利用 UE 的 Functional Test + Gauntlet 对技能效果做自动化验证</td>
</tr>
<tr>
<td><strong>Inventory 不应该和 GAS 紧耦合</strong></td>
<td>把库存系统抽象为独立模块，通过事件系统和 GAS 通信</td>
</tr>
<tr>
<td><strong>Combo 系统缺少”取消窗口”</strong></td>
<td>参考格斗游戏的 Cancel Window 设计，让连招手感和响应更灵敏</td>
</tr>
<tr>
<td><strong>GameplayCue 参数传递混乱</strong> — 有些走 EventData，有些走 TargetData</td>
<td>统一 Cue 参数规范，全部用 <code>FGameplayCueParameters</code> 的标准化字段</td>
</tr>
</tbody></table>
<hr>
<h2 id="后续计划"><a href="#后续计划" class="headerlink" title="后续计划"></a>后续计划</h2><p>Crunch 是一个”做给自己看”的 Demo——验证 GAS 架构思路，踩一遍网络同步的坑。下一个项目计划侧重：</p>
<ul>
<li><strong>联网多人战斗</strong> — Crunch 目前只有一个本地玩家，联网的实际体验还没验证</li>
<li><strong>更完整的 Combat Loop</strong> — 受击反馈、屏幕震动、HitStop、取消窗口</li>
<li><strong>技能配置工程化</strong> — 从”写 C++ 子类”迁移到”DataAsset 驱动 + 蓝图组装”</li>
</ul>
<hr>
<blockquote>
<p><strong>如果你也在做 UE 动作游戏，或对 GAS 感兴趣</strong>——欢迎交流。</p>
</blockquote>
]]></content>
      <categories>
        <category>Portfolio</category>
      </categories>
      <tags>
        <tag>Unreal</tag>
        <tag>GAS</tag>
        <tag>GameplayAbilitySystem</tag>
        <tag>Portfolio</tag>
        <tag>NetworkReplication</tag>
        <tag>CombatDesign</tag>
        <tag>ARPG</tag>
      </tags>
  </entry>
</search>
